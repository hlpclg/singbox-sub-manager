# Repository agent rules

回答务实、直接、简洁。

- 默认先给结论，只保留必要依据。
- 不寒暄，不复述问题，不写总结性结尾。
- 不使用 emoji，减少标题、列表和粗体。
- 简单问题控制在 1–3 句话；复杂问题只展开关键步骤、风险和限制。
- 不要为了简短而省略重要事实；需要详细说明时，用户会明确提出。

## 计划与约束

- 计划、实施、代码审查、修复闭环或正式交付前，必须按任务类型读取 `docs/agent-workflows/README.md` 及其路由到的工作流文档。
- 本仓库 `AGENTS.md` 和 `docs/agent-workflows/` 高于本机全局 Agent 工作流；与已批准规格或用户当前指令冲突时，停止并报告，不得自行取舍。

- 设计规范与计划中的“全局约束”高于各 Task；Task 不得覆盖上级约束。
- Task 与上级约束冲突、无法同时满足或存在歧义时，必须停止并报告，不得自行选择一种解释继续实施。
- 全局约束只定义一次。各 Task 只引用，不得复制或改写，以免约束漂移。
- 实施计划只描述契约、输入输出、不变量、验收测试、验证命令和提交范围；不得提供新的生产实现代码或规定非必要的内部 helper。
- 公共 API、稳定 schema、协议字段和必须保持兼容的符号可以在计划中精确定义。
- 可能阻塞、执行 I/O 或创建 goroutine 的路径必须覆盖成功、失败、取消和超时；不适用的场景须在计划中明确说明原因。
- 并发上限、资源释放、取消传播等逻辑不变量必须由确定性测试验证。`go test -race` 只能补充检测数据竞争，不能替代逻辑测试。
- 并发、context 取消、channel、锁、worker pool 和资源生命周期相关实现，必须由具备并发推理能力的实现者负责，并接受独立审查。

## 完成与验证

- Agent 的文字状态报告不是完成证据；完成状态必须由新鲜命令输出和 Git 差异共同证明。
- Go 命令必须按计划要求在 Go 1.22 容器中运行；本机 `go` 不在 PATH。
- 完成前至少执行计划规定的完整测试、`go test -race -count=1 ./...`、`go vet ./...` 和 `gofmt -l .`；`gofmt -l .` 必须无输出。
- Shell 文件发生变化时必须执行对应的 `shellcheck`。
- 不得削弱、删除或绕过既有测试和验证门禁来获得通过；修改门禁必须获得用户明确批准。
- 每次提交前必须核对 `git diff --check`、暂存文件清单和提交后的 `git status --short`、`git show --stat --oneline HEAD`。
- 在隔离且基线干净的 worktree 中，任务完成要求 `git status --porcelain` 为空。
- 在包含用户既有修改的工作区中，开始任务时记录 Git 基线；完成时只拒绝本任务新增的非预期或未提交改动，不得修改、暂存或删除用户既有文件。
- 声称“已提交”时必须提供 commit SHA，并证明预期文件包含在该提交中。
- 每个 Task 独立提交；未通过当前 Task 的规格符合性和代码质量审查，不得开始下一个 Task。

## 分支命名规范

- 开发任务必须加前缀，如 `feature/xxx`（例如：`feature/v0.4.0`）。
- Bug 修复必须加前缀，如 `bugfix/xxx`。
- 紧急修复必须加前缀，如 `hotfix/xxx`。
