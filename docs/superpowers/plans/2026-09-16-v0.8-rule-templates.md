# v0.8 规则模板与策略组 — 实施计划

> **Goal:** 把 `clash.yaml` 改为内置 14 组策略组模板，收紧 `proxyctl validate` 的结构校验，并交付准确的升级与订阅重新生成说明。
>
> **Architecture:** `internal/render` 以内部数据描述策略组、rule-providers 与规则，渲染确定、可复现；保留名冲突检查在任何文件系统操作前执行；`validate` 的 YAML 结构层只依据输入 YAML 的实际定义。Shell 兜底保留旧模板并明确告警。
>
> **Tech Stack:** Go 1.22、`gopkg.in/yaml.v3`（已在 `go.mod`）、Bash。
>
> **Spec:** `docs/superpowers/specs/2026-09-16-v0.8-rule-templates-design.md`（2026-09-16 第二轮修订后设计复审通过）。下文「设计 §x」均指该文档章节。

> **For agentic workers:** 每个 Task 遵循 `docs/agent-workflows/task-lifecycle.md` 的执行顺序：契约确认 → 验收矩阵 → 最小实施 → 聚焦测试 → 规格符合性审查 → 代码质量审查 → Task 专项门禁 → 计划与用户文档更新 → 主提交 → 状态封账提交 → 下一 Task。

**依据设计**：设计文档第 1–9 节。按根 `AGENTS.md`，本计划只描述契约、输入输出、不变量、验收测试、验证命令和提交范围，不提供生产实现代码，不规定非必要的内部 helper；下文精确定义的导出符号与 `validateClashYAML` 属于测试契约。

## 风险与编排

- 业务风险：中央 L1（设计 §8）。普通功能，不涉及认证、密钥、信任边界或服务端外部请求。每个 Task 由未参与实施的审查者独立终审，审查与复审按 `docs/agent-workflows/review-closure.md`。
- 流程复杂度：中等，2 个 Task 顺序依赖；Task 2 不改 Go 代码，依赖 Task 1 已封账。
- 并发与 I/O：不适用取消、超时与并发测试（设计 §7.4）。渲染为纯函数；`Write`、`merge`、`validate` 的文件 I/O 为本地同步调用，无 context、goroutine、channel、锁或网络调用。验收矩阵中相应列统一记为「不适用（设计 §7.4）」。

## 写计划前核对（2026-09-16）

以下为写计划时的事实核对，不是 Task 验收证据：

- 基线：`main` 的 `5e26383`（含 v0.7.1）；设计文档与本计划在 `feature/v0.8.0` 上作为一个文档提交交付（草稿阶段的修订提交已在推送前合并）。
- `cmd/proxyctl/main.go` 的 `cmdValidate` 把合法成员预置为 `DIRECT`、`REJECT`、`自动选择`、`节点选择` 与 `proxies`；渲染器单独加组会使 `TestRunMergeAndValidate` 失败——这是设计 §8 合并 Task 的依据。
- `render.Clash` 渲染传入的全部节点，不看 `Enabled`；`merge`、`validate`、`subscription build` 在调用前经 `nodes.Enabled` 过滤。`nodes` 解析器对旧格式及缺省 `ENABLED` 均置 `Enabled=true`，但 Go 字面量 `nodes.Node{}` 的零值为 `false`，测试构造节点须显式设置。
- `plan.md` 被 `.gitignore` 忽略，不进入版本控制；本次回写为本地更新。
- `internal/render` 当前无测试文件；README 无 `validate` 章节（`grep -n validate README.md` 无输出）。
- `tests/test_install_proxyctl.sh` 的 `verify_shell_fallback` 覆盖 Shell 兜底路径；`tests/test_install_monitor.sh` 第 12、26 行断言 `PROXYCTL_VERSION` 默认值。
- v0.7.1 发布后回归的教训：此前只在容器 root 身份下跑过完整测试，CI 为非 root。本计划的 Go 专项门禁同时要求非 root 身份全量测试。
- 设计 §4.1 的 20 个规则集 URL 均返回 HTTP 200；条目合计约 14.9 万（仅作说明，不作为测试输入，测试无网络）。

## Global Constraints（优先于各 Task，Task 不得覆盖）

1. **范围冻结**：设计 §2 非目标一律不做。不得修改 `internal/nodes`、`internal/health`、`internal/monitor`、`internal/backup`、`Shadowrocket` 输出、`nodes.ValidateName`、`install-proxy.sh update`/`rollback` 流程与 Shell 兜底模板内容。需要改动其中任何一项，停止并报告。
2. **模板契约**：14 组的名称、顺序、类型、成员与默认出口按设计 §3.1、§3.2；rule-providers 按设计 §4.1；规则逐条按设计 §4.2；`proxy-groups:` 之前的全部输出与 v0.7.1 逐字节相同（设计 §4.3）。
3. **保留名契约**：按设计 §3.3。只检查 `Enabled=true` 的节点；完全相等匹配；冲突时 `Write` 不做任何文件系统操作。
4. **校验契约**：按设计 §5。YAML 结构层判断「组是否存在」只依据输入 YAML 的 `proxy-groups`，不得引用 `render.ReservedNames()` 或任何渲染器内部数据。
5. **独立期望**：设计 §7.1 R1–R6 的期望值以字面量写在测试文件内，按设计 §3、§4 抄录；不得调用渲染器导出或内部的数据来生成期望。R7 的基准字面量取自 v0.7.1 渲染输出（见 Task 1 步骤）。
6. **既有测试**：`cmd/proxyctl/main_test.go` 与 `tests/*.sh` 的既有断言不得删弱或放宽。唯一预见的既有断言改动是 `tests/test_install_monitor.sh` 两处版本字符串 `v0.7.1` → `v0.8.0`（设计 §6.3、§7.3），在 Task 2「规格偏差」中列出。出现其他需要改动的既有断言，停止并报告。
7. **可测试性重构**：`validate` 的 YAML 结构层抽为接收字符串的函数（设计 §6.2），行为等价由既有 `TestRunMergeAndValidate`、`TestRunValidateFailure` 原样通过证明；不得为测试新增其他生产抽象。
8. **验证环境**：Go 命令在 `golang:1.22` 容器执行（本机 `go` 不在 PATH，不作为证据）；ShellCheck 使用与 CI 相同的 Ubuntu 24.04 apt 版本。
9. **提交与分支**：全部工作在 `feature/v0.8.0` 完成，经一个 PR 合并。每个 Task 独立主提交与状态封账提交；未通过规格符合性与代码质量审查、专项门禁与文档更新，不得进入下一 Task。提交不加 `Co-Authored-By` trailer（中央提交规范）。推送、PR、合并、tag 与 Release 均需用户另行明确授权。
10. **用户文档同步**：改变用户可见行为的 README 内容进入对应 Task 的主提交；状态封账提交回填本计划完成记录。`plan.md` 被 `.gitignore`（第 11 行）忽略，只在主 checkout 本地维护：封账时同步更新其「当前实施基线」v0.8 行状态与「后续优先级」，不暂存、不提交，并在完成记录中注明已同步。

## 统一验证命令

以下命令在各 Task 中按名称引用：

- **聚焦**：`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 go test ./internal/render ./cmd/proxyctl -run '<pattern>' -count=1 -v`
- **Go 专项门禁（root）**：`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 sh -c 'go vet ./... && test -z "$(gofmt -s -l .)" && go test -race -count=1 ./... && GOOS=darwin go build ./...'`
- **Go 专项门禁（非 root）**：`docker run --rm -u 1001:1001 -e HOME=/tmp -e GOCACHE=/tmp/.gocache -e GOPATH=/tmp/go -v "$PWD":/w -w /w golang:1.22 go test -count=1 ./...`
- **Shell 回归**：`docker run --rm -v "$PWD":/mnt -w /mnt ubuntu:24.04 sh -c 'apt-get update -qq && apt-get install -y -qq shellcheck && shellcheck install-proxy.sh merge-nodes.sh tests/test_install_json.sh tests/test_install_proxyctl.sh tests/test_install_monitor.sh tests/test_install_update.sh'`；`bash -n install-proxy.sh && bash -n merge-nodes.sh`；`bash tests/test_install_proxyctl.sh && bash tests/test_install_json.sh && bash tests/test_install_monitor.sh && bash tests/test_install_update.sh`
- **Git 门禁**：`git diff --check`、`git diff --cached --name-only`、`git status --short`、`git show --stat --oneline HEAD`

---

## Task 1: 渲染模板、保留名冲突与 validate 校验契约

**Stage:** 实施。**依赖：** 设计已批准。

**范围（设计 §3、§4、§5、§6.1、§6.2、§7.1、§7.2）**

- Modify：`internal/render/render.go`（可按职责拆出同包文件，例如模板数据与冲突检查）
- Modify：`cmd/proxyctl/main.go`（`cmdValidate`；可把结构层放入同包新文件）
- Create：`internal/render/render_test.go`
- Modify 或 Create：`cmd/proxyctl/main_test.go` 或同包新测试文件
- Modify：`README.md`（见本 Task「README」）

**接口契约**

- `func Clash(ns []nodes.Node) string`：签名不变；仍渲染传入的全部节点（调用方负责按 `Enabled` 过滤，与 v0.7.1 相同）。
- `func Shadowrocket(ns []nodes.Node) string`：不变。
- 新增 `func ReservedNames() []string`：每次返回新切片，内容与顺序为设计 §3.2 表中 14 个组名按 1–14 顺序，后接 `DIRECT`、`REJECT`，共 16 项。修改返回值不影响后续调用。
- 新增 `func CheckNodeNames(ns []nodes.Node) error`：忽略 `Enabled=false` 的节点；按输入顺序遇到第一个名称与保留名完全相等的启用节点时返回非 nil 错误，错误文本包含该节点名；否则返回 nil。
- `func Write(output string, ns []nodes.Node) error`：签名不变；先调用 `CheckNodeNames`，出错时直接返回该错误，此前不调用任何文件系统操作（包括 `MkdirAll`）。
- `cmd/proxyctl` 包内新增 `func validateClashYAML(doc string) error`（测试契约）：实现设计 §5 结构层 1–6，失败返回描述性错误，通过返回 nil。不读取渲染器数据。
- `cmdValidate`：节点层保留现有检查，在其后调用 `render.CheckNodeNames`；随后以 `render.Clash` 输出调用 `validateClashYAML`；失败时 stderr 以 `validation failed: ` 开头并返回 1；`sr.txt` 校验与成功输出不变。删除预置的 `自动选择`、`节点选择` 成员集合。
- 规则目标解析：`MATCH` 规则取第 2 个逗号分段，其他规则取第 3 个；分段数不足（`MATCH` 少于 2 段、其他少于 3 段）视为失败。

**实施步骤（先测试、后实现）**

1. 在改动 `render.go` 之前，以当前代码渲染 R7 固定输入（两个启用节点，建议名称 `JP-HY2`、`SG-HY2`，字段取固定值），截取 `\nproxy-groups:` 之前的全部内容作为 R7 基准字面量写入测试；此时运行 R7 应通过。在完成记录中写明基准的生成方式与基线提交 `5e26383`。
2. 写 R1–R6、R8–R11 与 C1–C10 测试；运行聚焦测试，确认新增测试因功能未实现而失败或因 `ReservedNames`、`CheckNodeNames`、`validateClashYAML` 未定义而编译失败，记录失败输出摘要。C11 为既有测试，不新增。
3. 实现渲染模板与冲突检查；运行 render 聚焦测试至通过。
4. 实现 `validateClashYAML` 与 `cmdValidate` 改动；运行 cmd 聚焦测试至通过。
5. 更新 README，运行 Go 专项门禁（root 与非 root）。
6. 规格符合性审查、代码质量审查（独立审查者），按审查发现闭环后执行 Git 门禁、主提交、状态封账提交。

### 验收矩阵

测试编号沿用设计 §7。R2 以 `merge` 生成的 `clash.yaml` 验证（生产路径经 `nodes.Enabled` 过滤），放在 `cmd/proxyctl` 测试中；其余 R 项在 `internal/render` 测试中。

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| 14 组名称与顺序（设计 §3） | 组缺失或顺序漂移，客户端展示与文档不符 | 恰 14 组且顺序等于字面量 | — | 不适用（设计 §7.4） | 不适用 | R1 | 聚焦 | 待执行 |
| 组类型与成员（设计 §3.1、§3.2） | fallback 缺节点、服务组候选错误、禁用节点泄漏 | `nodes.conf` 含 2 启用 1 禁用，`merge` 输出每组 `type` 与成员逐项等于字面量；禁用节点不出现 | — | 不适用 | 不适用 | R2 | 聚焦 | 待执行 |
| 默认出口（设计 §3.2） | 国内服务默认走代理或反之 | 11 个服务组首成员等于约定 | — | 不适用 | 不适用 | R3 | 聚焦 | 待执行 |
| 完整规则列表（设计 §4.2） | 规则遗漏、目标错误 | `rules` 与设计 §4.2 的 44 条字面量逐条相等 | — | 不适用 | 不适用 | R4 | 聚焦 | 待执行 |
| 顺序约束（设计 §4.2） | YouTube 被 Google 显式规则截走；GitHub/AI 落入 Microsoft | 流媒体 < 全部 Google 目标规则；AI服务、GitHub < Microsoft；Microsoft < 游戏；服务规则 < `RULE-SET,private,DIRECT` | — | 不适用 | 不适用 | R5 | 聚焦 | 待执行 |
| rule-providers（设计 §4.1） | URL/behavior 错误导致规则集为空 | 20 项名称、`type`、`behavior`、`url`、`path`、`interval` 等于字面量 | — | 不适用 | 不适用 | R6 | 聚焦 | 待执行 |
| 不变部分（设计 §4.3） | 误改 tun/dns/proxies | `proxy-groups:` 之前内容等于 v0.7.1 基准 | — | 不适用 | 不适用 | R7 | 聚焦 | 待执行 |
| 确定性 | map 遍历导致输出抖动 | 同一输入两次渲染逐字节相同 | — | 不适用 | 不适用 | R8 | 聚焦 | 待执行 |
| 保留名冲突（设计 §3.3） | 节点名与组名同名，客户端配置加载失败 | `ReservedNames()` 恰 16 项；非保留名通过 | 16 个保留名分别作为启用节点名 → 错误含该名 | 不适用 | 不适用 | R9 | 聚焦 | 待执行 |
| 禁用节点不参与冲突 | 禁用节点误报阻断 merge | 保留名仅在禁用节点 → `CheckNodeNames` 返回 nil | — | 不适用 | 不适用 | R10 | 聚焦 | 待执行 |
| 冲突时不写入 | 冲突导致订阅被截断或目录被创建 | — | 预置 `clash.yaml`、`sr.txt` 后 `Write` 冲突：两文件字节不变；输出目录不存在时仍不存在 | 不适用 | 冲突路径无任何文件系统副作用 | R11 | 聚焦 | 待执行 |
| merge 冲突（CLI） | 用户看不到冲突原因 | 保留名仅在禁用节点 → 返回 0（C2） | 启用节点为保留名 → 返回 1、stderr 含节点名、预置文件字节不变（C1） | 不适用 | 同上 | C1、C2 | 聚焦 | 待执行 |
| validate 节点层冲突 | validate 放过冲突 | — | 启用节点为保留名 → 返回 1、stderr 以 `validation failed: ` 开头 | 不适用 | 不适用 | C3 | 聚焦 | 待执行 |
| 结构层：组存在性只看 YAML | 以渲染器名称表冒充已定义组 | — | 成员引用 `故障转移` 但 YAML 无该组 → 失败（C4）；规则目标 `流媒体` 但 YAML 无该组 → 失败（C5）；规则分段不足 → 失败（C5） | 不适用 | 不适用 | C4、C5 | 聚焦 | 待执行 |
| 结构层：成员与名称 | 未定义代理、同名冲突漏检 | — | 成员引用未定义代理（C6）；代理名与组名相同、代理名为 `DIRECT`（C7） → 失败 | 不适用 | 不适用 | C6、C7 | 聚焦 | 待执行 |
| 结构层：规则集引用 | 引用缺失或冗余 provider | — | RULE-SET 引用未定义 provider（C8）；存在未引用 provider（C9） → 失败 | 不适用 | 不适用 | C8、C9 | 聚焦 | 待执行 |
| 结构层正向 | 新校验误伤正常输出 | `validateClashYAML(render.Clash(正常节点))` 返回 nil | — | 不适用 | 不适用 | C10 | 聚焦 | 待执行 |
| 既有 CLI 行为 | 重构改变 merge/validate 既有行为 | 既有测试断言原样通过 | 既有失败用例仍失败 | 不适用 | 不适用 | C11（`TestRunMergeAndValidate`、`TestRunValidateFailure`、`TestSubscriptionBuildEqualsMerge`、`TestMergeExcludesDisabled`、`TestMergeAllDisabledErrors` 等） | 聚焦 | 待执行 |
| 全量回归 | 其他包或非 root 环境回归 | root 与非 root 全量通过；vet、gofmt、darwin 构建通过 | — | 不适用 | 不适用 | 全部 | Go 专项门禁（root、非 root） | 待执行 |

**README（进入主提交）**：

- 「FlClash 使用方法」第 7 步与「配置默认包含」：列出 3 个基础组与 11 个服务组及各自默认出口（设计 §3.1、§3.2），说明服务组可在客户端单独切换；删除或改写「OpenAI、Claude、Google、GitHub 等优先代理规则」为与新分组一致的表述。
- 「合并多个节点」或「节点管理（proxyctl node）」：说明启用节点名不得等于 16 个保留名，冲突时 `merge`/`validate` 失败并指出节点名；禁用节点不受影响。
- `validate`：README 无对应章节，校验契约变化不另写用户文档；完成记录写「不适用」及审计依据（`grep -n validate README.md` 无输出）。

**提交范围**：上述文件。主提交消息建议 `feat(render): add built-in policy group template and structural validation`；封账提交 `docs(plan): seal v0.8 Task 1 completion record`。

## Task 1 完成记录

- 状态：`待实施`
- 实际范围：
- 规格偏差：
- 主提交 SHA：
- 状态封账：`待提交`
- README：
- 聚焦测试：
- Task 专项门禁：
- 复用或省略的验证：
- 独立终审：
- 未解决风险：

---

## Task 2: Shell 降级告警、版本固定与升级文档

**Stage:** 实施。**依赖：** Task 1 已封账。

**范围（设计 §6.3、§7.3）**

- Modify：`install-proxy.sh`（第 56 行 `PROXYCTL_VERSION` 默认值；`write_subscriptions_with_shell` 完成日志；`run_proxyctl_merge` 降级日志）
- Modify：`merge-nodes.sh`（第 11 行 `PROXYCTL_VERSION` 默认值）
- Modify：`tests/test_install_monitor.sh`（第 12、26 行版本断言）
- Modify：`tests/test_install_proxyctl.sh`（`verify_shell_fallback` 新增告警断言）
- Modify：`README.md`

**契约**

- `PROXYCTL_VERSION` 默认值在两个脚本中均为 `v0.8.0`，仍允许环境变量覆盖。
- Shell 兜底模板内容逐字节不变（可用 `git diff` 对 heredoc 区段核对）；降级触发条件不变。
- 降级告警：`run_proxyctl_merge` 走 Shell 渲染器时经 `log_warn` 输出，文本须同时包含 `shell renderer` 与 `no service policy groups`，并提示获取 proxyctl `${PROXYCTL_VERSION}` 后重新生成订阅；`write_subscriptions_with_shell` 的完成日志须包含 `no service policy groups`。
- `verify_shell_fallback` 捕获 `run_proxyctl_merge` 的输出并断言含 `no service policy groups`；原有断言不变。

**README（进入主提交）**

- 「升级与回滚」新增 v0.8.0 订阅重新生成小节，内容按设计 §6.3：
  - `install-proxy.sh update` 只升级二进制，不重新生成订阅。
  - 方式一：下载 `https://raw.githubusercontent.com/hlpclg/singbox-sub-manager/v0.8.0/merge-nodes.sh`（固定 tag，不用 `main`），`chmod +x` 后 `sudo ./merge-nodes.sh`。
  - 方式二：`sudo /usr/local/bin/proxyctl merge --nodes /etc/singbox-sub-manager/nodes.conf --output "/var/www/proxy-sub/$(sudo cat /var/lib/singbox-sub-manager/token)"`；已有订阅文件原地覆盖，保留原属主与权限。
  - 警告：旧版 `merge-nodes.sh` 内置 `PROXYCTL_VERSION=v0.7.1`，发现本机版本不同时会下载 v0.7.1 覆盖 `/usr/local/bin/proxyctl`，导致降级且订阅仍为旧模板。
- 「合并多个节点」中「要求项目已发布对应的 `v0.6.0`」改为与当前固定版本一致的表述。
- 发布顺序沿用 v0.7.1：tag 与 Release 资产在合并之后发布；资产确认可下载前，README 中的 v0.8.0 URL 不作为已验证入口（发布后待验证）。

### 验收矩阵

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| 版本固定 | 新装或 merge-nodes 拉回 v0.7.1 | 两脚本默认值与测试断言均为 `v0.8.0` | — | 不适用（仅改默认值） | 不适用 | `tests/test_install_monitor.sh` | Shell 回归 | 待执行 |
| 降级告警 | 用户不知道订阅缺服务组 | Shell 兜底路径输出含 `no service policy groups` | — | 不适用（日志文本变更，不改流程） | 不适用 | `verify_shell_fallback` | Shell 回归 | 待执行 |
| Shell 模板不变 | 误改兜底模板 | heredoc 区段 `git diff` 无变化 | — | 不适用 | 不适用 | Git diff 审查 | Git 门禁 | 待执行 |
| 升级文档准确 | 用户用旧脚本导致二进制降级 | README 含两种方式、完整命令与降级警告；v0.6.0 表述已修正 | — | 不适用 | 不适用 | 文档审查（逐项对照设计 §6.3） | Git 门禁 | 待执行 |
| 全量回归 | 脚本改动引入回归 | ShellCheck 无输出、四套 Shell 测试通过；Go 代码未改，Go 门禁复跑通过 | — | 不适用 | 不适用 | 全部 | Shell 回归、Go 专项门禁（root、非 root） | 待执行 |

**提交范围**：上述文件。主提交消息建议 `chore: pin proxyctl v0.8.0 and document subscription regeneration`；封账提交 `docs(plan): seal v0.8 Task 2 completion record`。

## Task 2 完成记录

- 状态：`待实施`
- 实际范围：
- 规格偏差：（预见：`tests/test_install_monitor.sh` 两处版本字符串 `v0.7.1` → `v0.8.0`）
- 主提交 SHA：
- 状态封账：`待提交`
- README：
- 聚焦测试：
- Task 专项门禁：
- 复用或省略的验证：
- 独立终审：
- 未解决风险：

---

## 完成检查（每个 Task 交接前）

- 验收矩阵所有适用项已有新鲜命令输出证据。
- 独立期望约束（Global Constraints 第 5 条）已由审查者核对：测试期望未引用渲染器数据。
- Go 专项门禁在 root 与非 root 两种身份下均通过，`gofmt -s -l .` 无输出。
- 涉及 Shell 文件的 Task 已用 Ubuntu 24.04 apt 版 ShellCheck 检查并运行四套 Shell 回归。
- 既有测试断言未删弱；改动已在「规格偏差」逐条列出。
- 主提交与状态封账提交已完成；本地 `plan.md`（Git 忽略）v0.8 行状态已同步；`git status --short` 为空。

## 发布后待验证（不属于任何 Task 的完成条件）

- FlClash（Mihomo 内核）导入 v0.8.0 订阅：14 组可见、服务组可切换；AI、YouTube、Telegram、Bilibili 各抽查一个域名命中对应组（设计 §7.5）。
- 真机按 README 升级步骤执行一次，确认 `proxyctl version` 为 `v0.8.0`，二进制未被降级。
- v0.8.0 tag 下 `merge-nodes.sh` URL 可下载。
