# v0.10 Reality 节点安装实施计划

> 执行者：按仓库工作流逐 Task 实施、独立审查、提交和封账。本文只定义契约、验收与提交范围，不提供生产实现代码；不自动启动子代理实施或后续版本工作。

Task-ID：v010-plan；Stage：计划；日期：2026-09-25。

实施业务风险：L2（私钥、root 文件写入、并发一致性和崩溃恢复）；流程复杂度：高（Go/Shell、两个主机角色、两个架构及 systemd）。本轮仅编写计划，不执行实现或真机变更；实施范围依据上述定稿规格。

**目标**：交付独立 Reality 节点生命周期、订阅中心事务化发布、统一节点菜单与 bootstrap 首装/过渡升级。

**架构**：角色与实例状态只读识别；实例生命周期和发布共用一个持久化事务引擎。Go 承担生命周期、恢复、发布和交互，Shell 只负责 bootstrap 与已有入口的互斥保护；复用已有 nodes、render、health/remote 和文件锁机制。

**技术栈**：Go 1.22、Linux openat/flock、systemd、Bash、固定版本 sing-box；保持现有 Darwin 构建门禁。

**规格**：[v0.10 定稿](../specs/2026-09-18-v0.10-reality-node-install-design.md)，基线 `6a98e08b416af4d90be8193c0082ffb59a86b4ca`；代码基线 `main@df3a4f4`。计划开始时分支 `feature/v0.10.0`，工作区及暂存区干净。套件更新草稿不是本计划的实施依据。

**当前状态**：计划已通过独立审查（2026-09-25）；Task 1–14 均未实施。§17.3 可选的无参数主菜单及 `proxyctl status` 整体顺延，本文仅交付必选的 `proxyctl node` 菜单。

## 全局约束

以下约束只在这里定义，各 Task 引用编号，不覆盖或复制。实现者同时阅读定稿规格；本文的测试清单不缩减规格要求。

- G1 范围与兼容：规格 §2–§4、§14.6、§17–§18。仅独立节点机、单实例、IPv4 服务地址；OS 为 Debian 12、Ubuntu 22.04/24.04，架构 amd64/arm64。没有 `proxyctl update/rollback`、套件会话、旧脚本降级防护或归档导入。旧 CRUD 无 `--publish` 的解析、输出及自定义 `--nodes` 语义保持；既有测试不修改、不删除，若无法同时满足则停止报批。
- G2 CLI：规格 §4.5、§8、§10.1、§13。完整解析先于 root 检查、锁、网络或写入；严格模式不补问。新路径退出码 0/1/2/4/5 和修改边界以规格为准；角色行去 stderr，stdout 保持可粘贴；每块恰好一条可执行命令，参数单引号引用，最低版本 `v0.10.0`。旧 CRUD 保持原有退出码，新增锁/遗留事务拒绝为 1。
- G3 资产与兼容：规格 §3、§5–§6。仅管理资产表中的路径，锁文件不属于资产；未知 manifest schema 只读拒绝，动态状态兼容读取与显式事务迁移，不得由 show/bootstrap 迁移。共享 proxyctl、共享 sing-box 和 bootstrap 旧副本不属于实例。
- G4 事务：规格 §7 全部适用。journal 先登记后执行；原语、路径链、目录身份、回滚序列/完成标记、systemd 进度、提交点及墓碑模型统一；未知 journal schema 或篡改只读返回 5。实例和发布不得各写一套恢复协议。
- G5 路径安全：规格 §7.1 与 v0.7.1 §3.3、§6.5。可信根之下逐级 dirfd + O_NOFOLLOW；每次修改前按 journal 当前 dir_ids 复验。禁止直接用字符串路径绕过受管路径访问；保留规格明确的尽力检测边界。
- G6 锁：规格 §11、§12。L1→L2→L3、反序释放，等待各 30/30/10 秒；monitor 保留 TryLock 返回 3。角色复查、反向检查和遗留事务检查在相应持锁区间内；不得重复申请继承的 flock。保留 §11.4 已知旧路径，不声称全局串行。
- G7 生命周期与预算：规格 §9、§13。全部 I/O、外部命令及锁等待可取消、有超时、释放资源；取消后独立回滚预算 120 秒，committed 只向前清理。超时值统一引用规格 §13.2；假时钟验证时间边界，禁止真实等待 10/30/120 秒充当单元测试。
- G8 密钥与网络：规格 §8.1、§9、§16。私钥实时推导公钥；自检 E1/E2/E3 分离；禁止把私钥或原始生产凭据放进日志、夹具或证据。固定 unit 模板与私网/本机地址拒绝策略必须经 Task 1 真实实测，失败停止，不自行放宽。
- G9 发布：规格 §10。默认布局、分节 nodes.conf、非空启用集合；三个文件可恢复、每文件原子，不能宣称双订阅整体原子；发布不产生 .bak，旧 CRUD 仍保留 .bak；回滚以本地状态为成功条件，HTTPS 复验只告警。
- G10 验证与封账：遵守仓库 AGENTS.md 和 docs/agent-workflows。每 Task 先建立验收矩阵，RED→GREEN→规格审查→质量审查→专项门禁→文档→主提交→状态封账，当前 Task 未封账不得开始下一项。涉及锁、取消、进程和资源生命周期的任务接受独立并发审查，race 不替代确定性验证。
- G11 用户文档：行为所属 Task 同步更新规格 §18.2 指定文档；Task 14 仅重组首页和消除重复。任何扩大范围、规格歧义、既有测试必须变化、实测不能满足固定模板，停止受影响任务并报告。

## 固定上游依赖

选择正式版本 **sing-box 1.14.1**，不使用滚动 latest 或预发布。2026-09-25 从 [官方 Release](https://github.com/SagerNet/sing-box/releases/tag/v1.14.1) 的 [资产与 SHA256 清单](https://github.com/SagerNet/sing-box/releases/expanded_assets/v1.14.1) 核实：

| 架构 | 资产 | SHA256 |
|---|---|---|
| amd64 | `sing-box-1.14.1-linux-amd64.tar.gz` | `12cb2816b52febb356f6a885b740cc8758c3f30b8ae0ca8edba80f0d2d35343f` |
| arm64 | `sing-box-1.14.1-linux-arm64.tar.gz` | `6060b42fa84c5dcaeae1799af7f61b0f1ae4855d9d5ddc9e02baba17154b3ae2` |

下载基址为 `https://github.com/SagerNet/sing-box/releases/download/v1.14.1/`；目标成员分别为 `sing-box-1.14.1-linux-<arch>/sing-box`。以上是官方资产元数据核对，**尚未下载实测**。Task 1 必须计算实际下载哈希并验证配置/行为；不能把选定版本等同于已经兼容。更换固定版本或不能满足规格时记录证据并修订计划后再推进。

## 文件边界与依赖

| 边界 | 新建或主要修改位置 | 职责及已有可复用接口 |
|---|---|---|
| 状态/角色 | `internal/role/role.go`、`internal/realitynode/state.go`、`identity.go` | 只读角色、manifest 和身份解析；`health.ValidToken`、`nodes.Validate` |
| 锁编排 | `internal/hostlock/lock.go`、`cmd/proxyctl/backup.go`、`cmd/proxyctl/node.go`、两个旧脚本 | 复用 `monitor.NewFileLock`、`backup.AcquireWithRetry`；cmd restore 已持 L2 的 lease 继续复用 |
| 共用事务 | `internal/transaction/journal.go`、`paths_unix.go`、`steps.go`、`recovery.go` | 共用 journal、真实文件原语、恢复；沿用 `internal/backup/dirfd.go` 的机制和复验顺序，不改变旧 backup 格式 |
| 实例 | `internal/realitynode/download.go`、`config.go`、`systemd.go`、`selfcheck.go`、`service.go`、`export.go` | 私有 sing-box、配置、systemd、自检及生命周期；`remote.GenerateConfig(nodes.Node, int) ([]byte, error)` |
| 发布 | `internal/publish/publish.go`、`verify.go`、`internal/subscriptionvalidate/validate.go` | 内存变更/渲染/校验/发布；复用 `render.Clash`、`render.Shadowrocket`，提取已有 validate 校验供 CLI 和发布共享，保留旧入口 |
| CLI | `cmd/proxyctl/node_args.go`、`node_service.go`、`node_publish.go`、`node_menu.go` | 严格解析、结果映射、交互；兼容入口保留在 `node.go` |
| bootstrap | `bootstrap-node.sh` | 首装与显式 --upgrade，两者共用校验与原子安装能力 |
| 交付 | 两个 workflow、`README.md`、`SECURITY.md`、四份运维文档、`plan.md` | 同 tag assets、校验、文档和真实验收 |

这些文件名确定责任和审查范围，不规定非必要 helper 或新的 Go 公共 API。内部接口由所属 Task 在验收前确定并记录，后续任务只消费已审查的契约，不单方面修改它。Linux 专属路径使用平台文件边界；不能为 Darwin 构建引入不安全降级实现。

执行顺序固定为 Task 1→14。事务拆为正向与恢复两个可独立审查的交付，Task 5 通过之前不接入用户可调用的变更命令。没有可并行写入的实施工作包。

## 审查重点与规格映射

除规格显式测试外，以下输入特别容易遗漏，均纳入拥有它们的 Task：

| 风险条件 | 预期与测试归属 |
|---|---|
| DNS 返回多地址或 NAT 公网地址不在网卡上 | 全部解析地址进入拒绝集合，刷新只在 reinstall；T1/T6/T7 |
| exec 超时后子进程仍存活、持 fd 或监听端口 | 先终止并 Wait，再释放资源；T6/T12 |
| 同一路径多次修改且回滚再次中断 | 不接受非法进度，不重放已恢复文件，inode 不漂移；T4/T5 |
| hardlink/符号链接/相对路径指向默认 nodes.conf | 仍走默认锁范围，不能绕过遗留事务检查；T3 |
| 校验成功与 SIGTERM 同时发生 | 成功边界串行判定，不能升级成功后又回滚；T12 |

| 规格条目 | 拥有 Task | 必须留下的证据 |
|---|---|---|
| §3、§6、§8.1、§9 实测 | T1、T6 | 官方哈希、check、真实握手、拒绝规则、unit 实测记录 |
| §4.1–4.3、§5 | T2、T3、T7、T8 | 状态表、资产清单、未知 schema 只读测试 |
| §4.5、§10.1 | T7、T9 | 严格/兼容模式、完整输出块执行测试 |
| §7.1–7.3 | T4、T5 | 真实临时 FS、每原语中断、目录替换、非法 journal |
| §7.4–7.6、§13 | T5、T7–T9 | recover 再崩溃、回滚边界、提交后清理、资源释放 |
| §8、§9、§14.1–14.2 | T6–T8 | 全生命周期、自检分类、导出/purge |
| §10 | T9 | 发布幂等、三文件恢复、HTTPS 与本地边界 |
| §11、§14.3、§14.6 | T3、T9、T11–T12 | 真 flock、旧入口保护、既有测试原样通过 |
| §12、§18.4 bootstrap 条目 | T11、T12 | 决策表、兼容探测、rename 中断与修复、信号 |
| §14.4–14.5、§15 | T13 | 两套 workflow、资产唯一匹配、完整门禁、真机验收 |
| §16、§17.2、§18.1–18.3 | T10、T14 及行为所属 Task | 菜单、迁移确认、四步 README、升级及限制文档 |

## 验证命令与通用完成步骤

Go 命令只在 Go 1.22 容器运行。实施前记录镜像 digest、`go version` 和环境；开发机可定义以下会话命令（仅测试命令，不是生产实现）：

```bash
g122() {
  docker run --rm -v "$PWD:/src" -w /src golang:1.22-bookworm "$@"
}
```

真实 Linux 权限、flock、进程中断测试在容器内本地文件系统执行；macOS bind mount 不能充当 Linux 文件系统安全证据。可把当前工作树快照复制到容器工作目录，记录对应 Git diff 摘要，并把证据写回。非 root 夹具在 Linux 可写 `/run/lock` 环境运行；若不满足，按 G1 报告，不能改掉或跳过原测试。真实 systemd 门禁使用一次性目标 VM，普通 Docker 容器不能替代。

| 门禁 ID | 命令/要求 |
|---|---|
| V-Go | 各 Task 给出的 `g122 go test -count=1 ...`；选定测试必须存在并实际执行，禁止空匹配算通过 |
| V-Shell | 对本 Task 变化脚本及新增测试执行 `bash -n`、`shellcheck` 和每个对应 `bash tests/test_*.sh`；ShellCheck 使用与 CI 一致的 Ubuntu runner apt 来源，记录 `shellcheck --version` |
| V-Full | `g122 go test -count=1 ./...`；`g122 go test -race -count=1 ./...`；`g122 go vet ./...`；`g122 gofmt -l .`（零输出）；`g122 env GOOS=darwin go build ./...`；执行 ci.yml/release.yml 的全部 Shell 测试及静态检查 |
| V-Git | 提交前 `git diff --check`、`git diff --cached --check`、`git diff --cached --name-only`；提交后 `git status --short`、`git show --stat --oneline HEAD`；无无关改动 |

每 Task 的复选步骤均须完成 G10：写该 Task 的确定性测试并确认 RED（环境故障不是 RED）→实现契约→同组测试 GREEN→独立规格/质量审查→专项门禁→同步文档→V-Full（所有代码、测试、脚本或 workflow Task）→V-Git→独立主提交→立即更新末尾状态表并以 `docs: close v0.10 task N` 封账。纯实测和文档任务以修改前缺失的检查/证据及修改后的检查代替虚假的 RED/GREEN。T1–T13 的主提交/封账前均须完成 V-Full，不能用该 Task 的专项命令替代全仓 race/vet/gofmt/Darwin 构建和适用 Shell 检查；同一最终对象的一次成功 V-Full 可同时满足 Task 门禁和全分支交付，封账只改状态文字时不再重复。后续有代码、测试、依赖、配置、迁移或生成物变化则按影响范围判定失效并补跑，不为流程重复同一有效对象的测试。

## Task 1：固定 sing-box 的兼容性实测门禁

**范围/依赖**：G1、G7、G8；无前置 Task。产物是可重复的实测与固定夹具，不能开始生产生命周期编码。

**文件**：新建 `internal/realitynode/probe_integration_test.go`（`reality_integration` tag）、`internal/realitynode/testdata/probe/`、`tests/test_reality_probe.sh`、`docs/reality-node-validation.md`；新增测试同步接入两个 workflow 的语法、ShellCheck 和可执行检查（普通 CI 不伪装有真实 systemd）。

**输入/输出契约**：使用本计划固定 tarball、规格 unit/服务配置、现有 `remote.GenerateConfig` 生成客户端；输出实测环境、下载哈希、命令、无凭据结果和已验证夹具。夹具身份由测试临时生成。真实门禁显式启用，缺 VM/网络权限记阻塞，不能 skip 后宣称通过。

| 不变量/风险 | 成功 | 失败 | 取消/超时与清理 | 确定性/实测证据 |
|---|---|---|---|---|
| 固定上游与 OS | 两架构资产实际哈希等于固定值；目标 OS 可运行 | 哈希、版本、归档成员不符立即停 | 下载受限，临时数据清理 | `TestPinnedReleaseProbe`；资产清单 |
| unit 与协议 | 三种 OS 的 unit 原文可用，真实 E1→E2→E3 返回 nonce | check/握手失败不改模板或放宽字段 | 中断清理测试 unit、进程、目录 | `TestRealityUnitProbe`、`TestRealityDataPathProbe` |
| 拒绝规则 | 规格 §8.1 全目标被拒绝，唯自检例外通过 | 域名私网、NAT、本机目标任一个穿透即阻塞 | 请求有上限、服务清理 | `TestRealityRoutingProbe`，记录 IPv6 可用性 |

- [ ] 下载两种 tarball 并计算哈希；检查 archive 实际条目与规格 §6/§14.1 的提取规则能同时成立，有冲突直接报告。
- [ ] 在 Debian 12、Ubuntu 22.04/24.04 的一次性 root/systemd VM 上实测原 unit；记录 amd64/arm64 执行覆盖，至少各有一台真实目标环境。
- [ ] 用 Go 1.22 容器生成测试程序/夹具，在 VM 执行 check、握手、端口归属、私网和全部本机地址拒绝；不能用“目标不可达”代替规则拒绝，使用可达对照目标和 sing-box 可观测结果证明命中规则。
- [ ] 私网域名实测需要 resolve 动作时，按规格 §8.1 已授权范围加入并固化通过的配置夹具；除此之外的模板变化停止报批。
- [ ] 专项：`bash tests/test_reality_probe.sh --verify-assets`；用 `g122 env CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go test -c -tags reality_integration -o /src/realitynode.test ./internal/realitynode` 构建 amd64 测试程序，arm64 用相同命令将 GOARCH 改为 arm64；复制测试程序及已校验的对应 sing-box 到目标 VM 执行 `./realitynode.test -test.run '^Test(PinnedReleaseProbe|RealityUnitProbe|RealityDataPathProbe|RealityRoutingProbe)$' -test.v`；V-Shell。
- [ ] 独立审查证据并提交：`test: verify pinned Reality runtime and routing contract`。用户文档仅记录已实测范围，不宣称 v0.10 已实现。

## Task 2：只读角色、资产状态与身份契约

**文件**：新建 `internal/role/role.go`、`role_test.go`；`internal/realitynode/state.go`、`state_test.go`、`identity.go`、`identity_test.go`、`testdata/v0.10/`；新建 `docs/reality-node.md`，更新 `docs/troubleshooting.md`。

**依赖/接口**：消费 T1 验证范围；提供 G3 的角色三态、实例五态、资产验证、身份解析及公钥推导能力，全部只读，路径根可注入。manifest 字段严格沿用规格 §5.2，动态状态按 §5.1/§8 字段形成 schema=1 固定夹具，后续版本读取兼容测试保留这些夹具。不引入隐式修复。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 角色优先级 | §4.2 所有组合精确分类 | 不可读/异常逐项诊断 | 本地小文件读取；无网络/后台进程，计算部分不适用 | `TestRoleMatrix`，所有输入字节/mtime 不变 |
| 实例状态/资产独占 | §4.3 全状态、manifest 校验 | schema 2/额外冲突/错误类型不一致 | 无变更或锁等待 | `TestInstanceStateMatrix`、`TestUnknownSchemaReadOnly` |
| 身份配对与格式 | RFC 7748 向量、UUIDv4、8 字节 short_id | 非法编码/长度拒绝 | 纯运算不适用；随机源错误可注入 | `TestIdentityVectors`，私钥不出诊断 |

- [ ] 添加表驱动测试：token 15/16/128/129 和非法字符；四资产根全无/任一个残缺；事务/墓碑优先；manifest 不包含自身；L3 文件单独存在不是实例。
- [ ] 测试动态 schema、owner/mode、不可变资产哈希、保留身份状态；已安装 fixture 固定覆盖 secrets/instance/manifest 三文件的兼容读取。
- [ ] 实现并通过 `g122 go test -count=1 ./internal/role ./internal/realitynode`。
- [ ] 独立审查只读性与资产边界；文档记录状态诊断、误建 nodes.conf 和旧中心缺 install.json 的处理建议。
- [ ] 提交：`feat: detect host roles and validate Reality instance state`，按通用步骤封账。

## Task 3：共用锁编排与旧写入入口保护

**文件**：新建 `internal/hostlock/lock.go`、`lock_test.go`、`cmd/proxyctl/node_lock_test.go`、`cmd/proxyctl/backup_lock_test.go`、`tests/test_shared_guards.sh`；修改 `cmd/proxyctl/node.go`、`cmd/proxyctl/backup.go`、`install-proxy.sh`、`merge-nodes.sh`、两个 workflow；同步 `README.md`、`docs/cli-reference.md`、`docs/upgrade.md`、`SECURITY.md`。

**依赖/接口**：消费 T2 状态/角色判定，复用已有 flock 与 AcquireWithRetry；产出 G6 锁编排和旧入口守卫。CLI restore 已取得的 L2 lease 传给现有 restore，新增 L3 随整段写入/失败恢复持有，不能二次取 L2 或 L1；dry-run 保持只读。Shell 新检查/锁路径通过环境变量注入，默认值来自规格。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 跨进程互斥与排序 | 各入口拿正确锁，反序释放 | 获取后续锁失败释放已有锁 | 假时钟边界、真进程协调，取消立即退 | `TestHostLockOrder`、`TestHostLockCancel` |
| 默认路径识别 | SameFile、相对路径、符号链接、hardlink 等价 | 自定义路径不受默认守卫影响 | 补问期间不持锁 | `TestDefaultNodesLockMatrix` |
| 反向/遗留事务检查 | 无实例，仅 L3 文件可继续 | 有任一实例资产/事务/墓碑或发布遗留即拒绝 | 拿锁后复查，不先写 | `TestRestoreGuards`、Shell 守卫矩阵 |

- [ ] 新增真实 flock 协调测试，用 pipe/屏障控制进程，不靠 sleep；覆盖 L1 阻塞默认 CRUD、L2 不阻塞旧 CRUD、restore 等待 L3，以及先预检后状态被改变。
- [ ] backup 创建归档取 L2；验证更新前 backup 超时不会替换二进制；restore 保持旧快照/回滚 lease 生命周期。默认 CRUD 补问完成再取 L1 并检查 Reality 资产与发布事务。
- [ ] install/update/rollback 在 L1 下新增双向守卫；merge 脚本 L1→L2、遗留发布检查、共享二进制替换也在 L1 内。不改变 v0.9 update/rollback 其余行为。
- [ ] 专项：`g122 go test -count=1 ./internal/hostlock ./internal/backup ./internal/monitor ./cmd/proxyctl`；V-Shell，执行四个现有 `test_install_{proxyctl,json,monitor,update}.sh` 与新守卫测试；原 `merge-nodes-path-isolation` 必须实际通过。
- [ ] 文档同步 G6 旧路径限制、monitor 可能返回 3、backup 30 秒超时影响、默认 CRUD 新锁及 `.bak` 不变；独立锁审查。
- [ ] 提交：`fix: serialize shared writes and guard incompatible host roles`，封账。

## Task 4：共用 journal、安全路径和正向原语

**文件**：新建 `internal/transaction/journal.go`、`paths_unix.go`、`steps.go`、`journal_test.go`、`paths_test.go`、`steps_test.go`、`testdata/schema1/`；平台边界文件按构建需要放在同包；更新 `SECURITY.md`。

**依赖/接口**：消费 T3 已持锁的调用约定，事务引擎不抢占调用者锁。输出 G4/G5 的持久化 schema 与正向执行能力；在 T5 闭环前没有 CLI 写入口。复用 backup 的 dirfd 机制与安全前提，不改变 backup 公共语义；是否抽取最小共享代码由本 Task 证明必要性，不复制其业务回滚协议。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| journal 原子持久化 | 登记→started→原语→done | schema/进度非法只读拒绝 | 写入各点中断可识别 | `TestJournalDurability`、`TestProgressPrefix` |
| 祖先链与目录身份 | 自建/自删更新身份 | 链被替换不执行下一修改 | 精确 hook/子进程中断，无计时猜测 | `TestAncestorReplacement`、`TestDirectoryIdentity` |
| 原语状态集合 | §7.2 每种合法中间态接受 | 不合法集合/未登记临时名拒绝 | 逐系统调用故障注入 | `TestPrimitiveCrashStates`、`TestPathChainStates` |

- [ ] 在真实 Linux t.TempDir、umask 077 中测试每个文件/目录原语的前后状态、fsync/rename/chmod；非 root chown 目标用当前 uid/gid。
- [ ] 测试 journal 原始字段全集、正向/回滚/frontier 合法前缀；同路径连续 pre/post、多个 started、未知 status/schema 均覆盖。冻结合法 schema1 夹具，不把非法数据规范化后继续写。
- [ ] 覆盖可信根不安全、叶子符号链接、token/subscription_root 整目录替换、真实 inode 替换；hook 必须位于打开 dirfd 后、复验前；断言目标和 journal 未修改。
- [ ] 专项：`g122 go test -count=1 ./internal/transaction ./internal/backup`，Darwin 构建；独立路径安全/持久化审查。
- [ ] SECURITY 说明事务快照敏感、临时名范围及复验窗口；提交 `feat: add durable transaction primitives and path guards`，封账。

## Task 5：共用回滚、recover 与提交后清理

**文件**：新建 `internal/transaction/recovery.go`、`recovery_test.go`、`crash_test.go`；修改同包 journal/steps；更新 `docs/troubleshooting.md`、`SECURITY.md`。

**依赖/接口**：消费 T4 schema/原语，输出 running/rolling_back/committed 的恢复契约。实例方提供 systemd 状态恢复与旧 manifest 门禁；发布方不注册 systemd 动作。本 Task 用假 systemd 验证顺序，真实 FS 验证恢复，保持同一引擎。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 回滚计划一次登记 | 当前状态到事务前，父子依赖正确 | 校验失败前不得改 phase | 主取消后独立 120 秒上下文 | `TestRecoveryPlan`、`TestRollbackBudget` |
| 中断后只续做 | rollback_steps/dir_ids 收敛 | 篡改/非法标记只读 5 | recover 第 k 点再退出再恢复 | `TestRecoveryCrashMatrix` |
| 文件完成标记 | 不重复替换，inode 保持 | 完成后内容变化拒绝 | restoring 重做服务；restored 只门禁 | `TestRestoredExecutableInode` |
| commit/墓碑 | 向前清理、墓碑整体删除 | 无 journal 却有非允许内容拒绝 | 清理中取消不回滚 | `TestCommittedCleanup`、`TestTombstoneRecovery` |

- [ ] 逐原语/事务阶段运行子进程 crash 矩阵，父进程恢复并对比 bytes/mode/owner/存在性；journal.tmp 与 dl/snap 残留合法，不误判为外部篡改。
- [ ] 覆盖 remove_dir 反向 mkdir 在 umask 077 下的中间 mode、新 inode 登记前后、临时文件先于父目录删除；done 步骤不重放。
- [ ] 验证 stop→disable→恢复文件→daemon-reload→enable→restart→旧门禁；demoting 期间 disabled 不用正向 enable 状态判篡改。事务前不 active 时保持 stop，不要求端口门禁。
- [ ] 覆盖 files_done/restoring/restored 每个中断点、非法组合、frontier 非法序列；发布没有 systemd 时也遵守文件完成标记。
- [ ] 测试 abandon 全部路径处于事务前才成功，保留当前服务状态并告警；committed 不允许回滚。规格 §14.2 的“回滚后验证失败 5”按 §10.4 指本地状态不符，HTTPS 不可用只告警。
- [ ] 专项：`g122 go test -count=1 ./internal/transaction`；`g122 go test -race -count=1 ./internal/transaction`，独立恢复与取消审查；提交 `feat: recover transactions across rollback and cleanup interruptions`，封账。

## Task 6：实例配置、私有下载与自检资源生命周期

**文件**：新建 `internal/realitynode/download.go`、`config.go`、`systemd.go`、`selfcheck.go` 及对应 `_test.go`；更新 T1 夹具、`docs/reality-node.md`、`SECURITY.md`。

**依赖/接口**：消费 T1 实测夹具、T2 身份、T5 事务回调边界；输入实例参数/身份、目标私有版本、context；输出已校验下载/配置和分类自检结果。systemd、下载、命令、HTTP、时钟可注入；沿用 `remote.GenerateConfig`，其既有语义不变。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 下载与提取 | SHA、唯一目标普通文件≤200 MB、精确版本 token、check | 错哈希/条目/前缀版本拒绝 | 下载/check/version 时间上限，子进程回收 | `TestDownloadMatrix`，无受管资产变化 |
| 服务配置 | unit 字节等于 T1 模板、地址拒绝集合完整 | 参数/DNS/端口错误前置拒绝 | DNS 等 I/O 遵守主取消及有限等待 | `TestConfigAndAddresses`、`TestSelfcheckPortSelection` |
| E1/E2/E3 与分类 | nonce 200；出口 204 | E2 竞争最多三次；E3 占用为本机服务失败 | 每个出口 kill+Wait、关闭 listener/HTTP body | `TestSelfcheckLifecycleMatrix` |

- [ ] 表驱动覆盖 IPv4/主机名/SNI、253/63 长度边界、IP SNI、IPv6、前缀 `-` 名称；从临时端口下限之外选 selfcheck，空范围/十次失败拒绝。
- [ ] 断言 DNS 所有 A/AAAA、全部非回环地址及 NAT server 地址进入拒绝集合；dest 用系统根与 TLS1.3，非 h2 只警告。
- [ ] 门禁分别伪造 active 但监听 PID 错、exe 错、只监听 IPv6、check 失败；10 秒轮询不得误通过。假 systemctl stop 推进 10 秒仍落在 15 秒预算，超时终止/回收。
- [ ] 自检成功/失败/取消/各超时都验证 E3 能重新绑定、客户端已 Wait、fd 不泄漏；§9.4 复用同一客户端，出口失败不阻塞注册；协议失败附一次 dest 复测。
- [ ] 专项：`g122 go test -count=1 ./internal/realitynode ./internal/health/remote`、本包 race；T1 固定配置实测如改变则重跑受影响部分。
- [ ] 同步私网例外、SNI 共用特征、地址变化需 reinstall 等限制；提交 `feat: prepare and verify private Reality runtime`，封账。

## Task 7：严格 CLI 与 install/reinstall/show

**文件**：新建 `cmd/proxyctl/node_args.go`、`node_args_test.go`、`node_service.go`、`node_service_test.go`、`internal/realitynode/service.go`、`service_test.go`、`output.go`、`output_test.go`；修改 `cmd/proxyctl/node.go`；更新 `docs/cli-reference.md`、`docs/reality-node.md`。

**依赖/接口**：消费 T2/T3/T5/T6；对外提供规格 §8.1–8.3 与 `node service --help`；内部命令结果区分修改前、已恢复、需介入，不靠错误字符串映射。严格参数扫描也供 T9 复用，旧解析路径保留。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 参数先完整解析 | 合法 flags 前后位置等价 | 错用法 2，无锁/网络/mtime 改动 | 纯解析不适用 | `TestStrictNodeArgs`，旧测试原样通过 |
| install/reinstall | 按操作提交点更新 manifest；身份不变 | 受管修改前 1，之后恢复为 4/5 | 预检/锁/事务/回滚/commit 五阶段信号 | `TestServiceLifecycleMatrix`、crash 子进程 |
| show 与输出 | 公钥实时推导、版本不同仅 stderr 提示 | 状态不符 1，无隐式迁移 | show 仅 L3、有界等待 | `TestServiceShowReadOnly`、`TestPasteCommands` |

- [ ] 严格解析全矩阵：重复/未知/缺值、布尔值、位置个数、-publish、--nodes 冲突、--abandon 位置、--help 混用；`--name --publish` 是值而非模式。旧 CRUD 重复 flag 最后值与无角色行不变。
- [ ] install 前置 OS/root/角色/磁盘/端口/dest，锁后复查；成功资产权限、unit、身份、私有版本和 manifest 全部断言；不写共享资产。
- [ ] 两种 reinstall 来源均覆盖 selfcheck 端口重选、刷新本机地址；复用已校验版本或显式版本+哈希成对参数；不允许 name/server/port/sni；两版本保留和 committed 后旧版本清理。
- [ ] 两种 show、两种 reinstall、install 输出在临时 shell 整块执行，假 sudo/proxyctl 记录恰好一条调用、单引号转义与空 short_id；不执行真实跨机注册。
- [ ] 生命周期每步 crash 后用引擎恢复并比较前状态；测试无新 manifest 提前提交。show 输入文件 bytes/mtime/owner/mode 不变。
- [ ] 专项：`g122 go test -count=1 ./cmd/proxyctl ./internal/realitynode ./internal/transaction`，受影响并发包 race；独立审查并提交 `feat: install reinstall and inspect Reality services`，封账。

## Task 8：uninstall/purge/export/recover

**文件**：修改 `internal/realitynode/service.go`、`cmd/proxyctl/node_service.go`；新建 `internal/realitynode/export.go`、`export_test.go`、`uninstall_test.go`、`cmd/proxyctl/node_recover_test.go`；同步 `docs/reality-node.md`、`docs/troubleshooting.md`、`SECURITY.md`。

**依赖/接口**：消费 T7 状态与命令入口、T5 恢复；交付规格 §8.4–8.6 的 CLI，复用同一 journal 和代码路径执行自动/显式恢复。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 保留身份卸载 | stop/disable 后删服务，保留三份状态 | 任一步故障完整恢复 | 独立回滚预算与清理 | `TestUninstallCrashMatrix` |
| purge 与备份 | 全资产消失；仅显式归档可留密钥 | PATH 存在不覆盖，归档失败不开始事务 | 导出中断清除临时文件；O_EXCL/link 防竞争 | `TestPurgeExportMatrix` |
| recover/abandon | 明确状态入口；成功 0 | 未完成以外拒绝；篡改 5 保留现场 | 重复中断仍收敛 | `TestServiceRecoverMatrix` |

- [ ] 两种 purge 来源、uninstall 每步骤故障/崩溃；快照包含全部实例资产；已卸载身份状态不生成新密钥。
- [ ] 导出仅在 L3 复查后、事务前；archive mode0600，内容仅规格两目录；模拟目标名竞争、写入失败、取消、fsync/link 失败不覆盖用户文件。
- [ ] 验证 purge 无隐式私钥备份、没有事务/墓碑/状态目录，L3 锁可留，proxyctl 与 bootstrap previous 保留并提示。
- [ ] 显式 recover 覆盖 committed、墓碑、无 journal 两种合法初始残留；abandon 不重启、不执行门禁但输出告警。stdout disable/remove 推荐与备选保持单条可执行。
- [ ] 专项：`g122 go test -count=1 ./internal/realitynode ./internal/transaction ./cmd/proxyctl`，同步归档不支持解压恢复/导入说明。
- [ ] 独立恢复审查，提交 `feat: uninstall purge and recover Reality services`，封账。

## Task 9：订阅中心事务化发布与 --publish CRUD

**文件**：新建 `internal/publish/publish.go`、`verify.go`、`publish_test.go`、`recovery_test.go`、`internal/subscriptionvalidate/validate.go`、`validate_test.go`、`cmd/proxyctl/node_publish.go`、`node_publish_test.go`；修改 `cmd/proxyctl/node.go`、`validate.go`（保留原校验包装入口）、必要的 `main.go` 校验接线；更新 `README.md`、`docs/cli-reference.md`、`docs/troubleshooting.md`、`SECURITY.md`。

**依赖/接口**：消费 T3/T5/T7；输入标准布局、内存节点变更与 Version，输出已验证的三文件发布或可恢复事务。校验与现有 validate 同源；`render.Write`、`nodes.WriteFile` 的旧语义不改。HTTPS 复用 health 的连接/TLS 策略，不依赖仅返回健康布尔值的接口来替代正文哈希校验。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 预检及幂等 | 相同 add/已启用 enable 等直接发布 | legacy/空启用集合/缺 token 目录/旧版本拒绝 | L1/L2 等待取消清理 | `TestPublishMutationMatrix` |
| 渲染与替换 | staging 在 Web 根外、三个文件逐个替换 | 渲染前后失败分别 1 或恢复4/5 | 每文件 HTTPS10秒、独立回滚 | `TestPublishFailureBoundary` |
| 恢复结果 | 本地 bytes/metadata/存在性恢复 | 本地不符5；HTTPS复验失败仅告警 | crash/recover 再 crash 不重写已完成文件 | `TestPublishRecoveryMatrix` |

- [ ] 定义所有幂等/拒绝组合，特别是 disabled 同字段 add、不同字段同名 add、缺失 edit/enable/disable、移除最后启用节点，拒绝路径断言无变更。
- [ ] dev/不规范版本拒绝，数字段比较包含 v0.10.10/v0.10.9；只使用 install.json 的域名/token/订阅根以及固定默认 nodes.conf；发布不创建中心身份文件或 token 目录。
- [ ] 渲染/validate 与现有结果一致：节点验证、启用集合、名称冲突、CHANGE_ME、Clash 结构、Shadowrocket 非空/行数/URI scheme 均由共享校验覆盖，旧 CLI 输出不变；最终 write_file 临时名在各目标目录，不从 staging 跨文件系统 rename；nodes.conf0600，订阅0644并按规格继承属主；不存在用 caddy。
- [ ] 覆盖三文件每步中断、原本不存在、外部篡改/祖先替换、未登记 temp、staging mkdir/remove 同路径链、无 .bak；Caddy 故障本地回滚后事务删除且4，recover为0。
- [ ] 两份 HTTPS 都校验 200 和 body SHA，用 domain SNI/Host 连接 loopback；不把公网上同内容响应算通过。发布成功后仅清理，不回滚。
- [ ] 专项：`g122 go test -count=1 ./internal/publish ./internal/subscriptionvalidate ./internal/render ./internal/nodes ./internal/transaction ./cmd/proxyctl`；发布/锁 race；独立跨组件审查。
- [ ] 同步标准发布入口、原子性声明、.bak 差异、遗留 journal 人工出口和已知并发限制；提交 `feat: publish node changes with recoverable transactions`，封账。

## Task 10：独立交互菜单与 legacy 迁移确认

**文件**：新建 `cmd/proxyctl/node_menu.go`、`node_menu_test.go`；修改 `cmd/proxyctl/node.go`；更新 `README.md`、`docs/cli-reference.md`、`docs/reality-node.md`、`docs/troubleshooting.md`。

**依赖/接口**：消费 T7–T9 已稳定 CLI；输出 `sudo proxyctl node` 双角色菜单。菜单采集/确认后调用相同业务命令，不另建写入通路；本 Task 独立于生命周期提交，满足规格 §17.2。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| TTY/root/角色 | 双TTY菜单、不可用项说明原因 | 非TTY usage2；非root按规格提示 | EOF/取消前不持锁、不写入 | `TestNodeMenuTTYMatrix`（非 root 不读取角色，退出 1） |
| 隐藏路径与发布参数 | 操作默认完整发布 | 业务错误原样表达 | 确认前不持锁；确认后继承业务取消 | `TestMenuPublishFlow` |
| legacy 迁移 | 显示备份路径后确认迁移 | 拒绝/EOF不迁移；非交互拒绝 | 迁移失败不执行后续发布 | `TestMenuMigrationConsent` |

- [ ] 用可注入输入/TTY和确定性提示测试覆盖菜单、非root、角色不确定及状态不允许；未完成事务在角色行下显示 recover 命令，不新增菜单恢复项；公网 IPv4 探测只作默认，用户确认 server/SNI 才提交。
- [ ] 覆盖 add/edit/enable/disable/remove 都进入发布闭环，用户不输入 --nodes/--output/token；确认界面给等价命令，参数旗标在前。
- [ ] migrate 沿用已有备份语义与 T3 默认锁；备份位置确认在迁移前。拒绝或失败保留原文件，不悄悄继续注册。
- [ ] 专项：`g122 go test -count=1 ./cmd/proxyctl`，旧非TTY/CRUD测试全部保留；独立流程审查。
- [ ] 同步首次迁移必要性与菜单操作；提交 `feat: guide node management through publish-aware menus`，封账。

## Task 11：bootstrap 首装与安装文件修复

**文件**：新建 `bootstrap-node.sh`、`tests/test_bootstrap_node.sh`；修改两个 workflow；更新 `README.md`、`docs/reality-node.md`、`docs/cli-reference.md`。

**依赖/接口**：消费 T7 help/service 与 T10 菜单、T3 锁；脚本 `PROXYCTL_VERSION=v0.10.0`，输出/exec 形态严格遵循规格 §12 首次安装表。新增 --upgrade 留给 T12，不把本 Task 当成可发布版本。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 复用决策 | 同版本可信且有能力才复用；低于0.10可替换 | 坏sidecar不执行；有效较新版本不降级 | 下载每文件120秒，信号删临时文件 | Shell 全决策表 |
| 角色与二进制锁 | L1内复查、校验与原子替换 | 中心/不确定拒绝，无下载 | exec前关fd9，后续命令能取L1 | 真flock/exec测试 |
| 参数与TTY | 剥离replace，其余原样；双TTY进菜单 | 无参数非TTY usage2 | 退出锁/临时文件释放 | 参数逐字节记录 |

- [ ] 每决策表行建新增 shell 测试；假已安装二进制记录是否被执行，证明坏 sidecar 时零执行。
- [ ] 覆盖首装/能力缺失/dev/版本比较、--replace-proxyctl 只授权坏 sidecar 修复而非可信新版降级；root/OS/arch及角色检查。
- [ ] 假下载/校验失败、同目录 rename、二进制0755 sidecar0644、能力探测、临时文件清理；真实子命令 L1 重取成功证明无继承自锁。
- [ ] 专项：V-Shell；`bash tests/test_bootstrap_node.sh` 与 `bash tests/test_shared_guards.sh`；两个 workflow 实际执行新增测试。
- [ ] 同步“首次两步”、可选 checksums 校验、中心误用提示；提交 `feat: bootstrap standalone Reality nodes`，封账。

## Task 12：bootstrap --upgrade 与兼容检查/人工恢复

**文件**：修改 `bootstrap-node.sh`；新建 `tests/test_bootstrap_upgrade.sh`、`tests/test_bootstrap_restore.sh`；修改两个 workflow；更新 `README.md`、`docs/reality-node.md`、`docs/upgrade.md`。

**依赖/接口**：消费 T11、T8 的只读 show/状态模型；对外仅 `--upgrade` 唯一参数。兼容检查不迁移；旧副本仅最近一次、无会话；不引入自动降级命令。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| 候选状态兼容 | installed/uninstalled fixture show0且文件不变 | 未知schema/残缺/超时拒绝，安装与previous不变 | 子进程先回收再解锁 | `test_bootstrap_upgrade.sh`兼容矩阵 |
| L1/L3交接 | parent保留L1，child独占L3无fd9，重取后复查 | 资产/角色/事务变化或重取失败停止 | show15秒，重取L3 10秒；假时钟 | 真flock+进程屏障 |
| 双文件备份/替换 | old pair持久化且匹配后换新并验证 | 替换失败恢复4；恢复失败5 | 成功前信号回滚，成功后0 | 每rename/成功边界故障点 |
| 人工恢复/修复 | 文档步骤先核对hash再原子恢复 | 不匹配旧副本零执行/零覆盖 | SIGKILL后同目标脚本修复 | `test_bootstrap_restore.sh` |

- [ ] 升级表逐行测试，包括不存在、坏sidecar、dev、同版0、新版拒绝、旧版升级；同版无下载。角色/事务拒绝时不下载。--upgrade 与任意额外参数2。
- [ ] 候选验证严格顺序 SHA→version→help→show；四资产根全无才跳show，残缺不得跳；stdout丢弃、失败诊断保留。用 T2 schema1 fixture 和候选stub复现读取结果，另用真实当前 Go 候选读取夹具，避免仅证明stub约定。
- [ ] 子进程 barrier 测试 L1始终不可竞争、L3确实交接、show无fd9；检查前后 bytes/type/owner/mode/存在性复查；取消/超时结束后无进程和锁残留。
- [ ] 旧副本两文件之间中断当前安装不变；两个安装rename之间失败恢复4/5；SIGKILL产生不匹配pair，不执行它，同目标--replace修复。已安装实例+非TTY无参数修复结果正确但整体usage2，分开断言。
- [ ] 信号在下载、替换前、两rename间、验证前后每个位置注入；成功标记与trap串行。单纯show不关闭人工回退窗口，任何生命周期变更包括失败/取消/recover之后禁止直接换旧版，文档明确无法确认就停止。
- [ ] 专项：V-Shell 和三份 bootstrap 测试、shared_guards；构建当前候选所需 Go 命令用 g122；独立审查锁/子进程/trap覆盖与旧首装行为。
- [ ] 手动恢复步骤在临时根+真实flock中执行，坏/缺sidecar拒绝、正常pair权限正确；提交 `feat: upgrade node proxyctl with compatibility checks`，封账。

## Task 13：同 tag Release 集成与真实全流程验收

**文件**：修改 `install-proxy.sh`、`merge-nodes.sh`、两个 workflow、`plan.md`、`docs/upgrade.md`、`docs/reality-node-validation.md`；新建 `tests/test_release_assets.sh`、`tests/test_v010_flow.sh`。bootstrap 已固定同一版本。仅修集成根因；新功能范围回到所属 Task 重新审查。

**依赖/接口**：消费 T1–T12。交付可发布的统一版本与可审计验证结果；不创建 tag、不推送、不实际发布、不擅自操作生产主机。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与测试 |
|---|---|---|---|---|
| Release兼容 | 三脚本0.10、两架构binary、脚本assets和hash齐全 | v0.9解析多匹配/缺记录阻断 | 构建失败无发布；workflow权限保持 | `test_release_assets.sh`，保存dist清单 |
| 自动流程 | fresh→install→show→uninstall→reinstall→purge；中心迁移/注册/禁用/启用 | 各步骤输出、退出码、最终状态准确 | 子命令超时及中断无资源残留 | `test_v010_flow.sh`隔离集成 |
| 真机流程 | 规格§14.5两机+FlClash实际可用 | 路由拒绝/客户端失败阻断完成 | 故障记录、清理测试实例 | 脱敏记录，不能以mock替代 |

- [ ] 三脚本版本一致；release构建 linux-amd64/arm64，复制三脚本到dist，checksums中每文件恰好一条。用 `df3a4f4:install-proxy.sh` 原 v0.9 校验解析方式测试唯一二进制匹配，不修改既有测试。
- [ ] 核对 ci.yml 和 release.yml 都包含 bootstrap、两个旧脚本和所有新 shell 测试的 bash-n/ShellCheck/测试执行；T1真实门禁显式另行记录，不允许条件skip冒充通过。
- [ ] V-Full，加 dist 二进制 version精确匹配；对应平台运行验证，不以执行不了arm64而忽略校验。
- [ ] 在已授权的一次性订阅中心和节点 VM 执行规格 §14.5 全六步：migrate、首次两步、粘贴注册、FlClash出口、所有受限目标、卸载/disable/重装/enable、purge和角色菜单；无真机/客户端访问时准确标记未完成，不得进入 T14 的“流程稳定”前提。
- [ ] 记录 shellcheck来源/版本、Go镜像、OS/arch、精确Git对象、命令退出码、故障根因与清理；脱敏后更新验证文档与plan.md。独立全分支规格/质量终审，已有Task证据可复用，仅补失效部分。
- [ ] 提交 `build: package v0.10 scripts and verify deployment flow`，封账。

## Task 14：README 四步首页与文档一致性终审

**文件**：修改 `README.md`、`docs/cli-reference.md`、`docs/upgrade.md`、`docs/reality-node.md`、`docs/troubleshooting.md`、`SECURITY.md`、`plan.md`；本计划仅回填完成状态。

**依赖/接口**：T13真实流程稳定且已审查；按规格 §17.2 为发布前最后一个独立 Task。只重组前序已同步的行为文档，不增加功能、不首次补写遗漏的契约。

| 不变量/风险 | 成功 | 失败 | 取消/超时 | 持久化/清理与检查 |
|---|---|---|---|---|
| 普通用户主流程 | README只保留四步和必要链接，首次中心迁移明确 | 旧命令/过期路线图/重复示例查出即修 | 纯文档，不适用 | 逐步对照T13实测输出 |
| 运维信息完整 | 参数/升级/生命周期/排障各归一处 | 不宣称全局串行、整体原子或无条件降级 | 纯文档，不适用 | §15/§16/§18.2逐项链接核对 |

- [ ] 首页四步：中心安装并迁移→节点首次两步→中心粘贴推荐命令→客户端导入；菜单作为后续入口。
- [ ] 参数全集迁到cli-reference；v0.9→v0.10升级和同tag脚本限制迁到upgrade；节点升级/人工回退与修复的区别迁到reality-node；恢复现场处理迁到troubleshooting。
- [ ] 对照前序证据核查所有示例、参数、退出码、路径、备份含密钥说明、私网例外、地址刷新、monitor/backup副作用；删除已完成路线图与过期警告，保持安全限制可发现。
- [ ] 文档一致性独立审查；V-Git。纯文档不重跑未受影响的V-Full；只有 T13 被测提交/内容摘要、代码/测试/依赖/配置/生成物及验证环境均未改变，才能复用其精确SHA、命令、退出码和证据，记录本轮只改文档与省略理由；任何影响测试对象或环境的变化使对应证据失效，补跑后才能封账；若示例需改变代码，退回所属Task，不带缺口交付。
- [ ] 提交 `docs: simplify v0.10 setup and operations guides`，封账；发布/推送另需对应授权。

## 实施证据与封账记录

下表是计划状态，不能作为运行证据。每行封账时扩充实际范围/偏差、README或对应文档、RED/GREEN命令与结果、专项门禁、独立审查结论、主提交SHA、封账状态、复用依据和未解决风险；未经执行不得填“通过”。

| Task | 状态 | 实际范围/偏差 | 验证与审查 | 主提交 / 状态封账 | 用户文档 |
|---|---|---|---|---|---|
| 1 | 待实施 | 尚未实测 | 未执行 | 未提交 | 实测记录未创建 |
| 2 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 3 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 4 | 待实施 | 无实施改动 | 未执行 | 未提交 | SECURITY随主提交更新 |
| 5 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 6 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 7 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 8 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 9 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 10 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 11 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 12 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 13 | 待实施 | 无实施改动 | 未执行 | 未提交 | 随主提交更新 |
| 14 | 待实施 | 无实施改动 | 未执行 | 未提交 | 重组已有内容 |

计划编写证据：基线 `6a98e08`，只新增本计划，未修改规格、代码、测试或依赖；官方固定资产元数据已核实。2026-09-25 完成任务顺序、占位符、行末空白、相对链接检查；独立计划审查发现的 V-Full 逐 Task 封账要求已补齐并通过定向复审，无未闭环项。没有运行 Go/Shell/systemd 门禁，也未复用或宣称已有运行证据；本轮只新增 Markdown，不影响运行时，实施门禁属于尚未执行的 Task。用户文档不适用本次计划提交，实际行为文档已分配到各 Task。

计划编写封账：主提交 `fb94529`（`docs: plan v0.10 Reality node implementation`），只含本计划 397 行新增。主提交前 `git diff --check`、`git diff --cached --check` 均通过，暂存文件仅本计划；提交后 `git status --short` 为空，`git show --stat --oneline HEAD` 确认范围。状态封账以紧随其后的 `docs: close v0.10 implementation planning` 提交识别；此提交仅增加本段证据，不改变实施契约。未推送，Task 1–14 仍全部待实施。
