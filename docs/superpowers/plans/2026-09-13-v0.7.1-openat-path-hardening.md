# v0.7.1 恢复路径 openat 加固 — 实施计划

> **Goal:** 把 `internal/backup` 在可信根之下的创建、替换、删除和读取改为相对逐级打开的目录 fd，落实设计文档第 3.3 节的无前提保证 1–4 与范围受限的检测能力，公开契约不变。
>
> **Architecture:** 可信根按现状跟随符号链接打开一次；根下名字逐级 `O_NOFOLLOW` 解析；变更前复核祖先链；失败清理按记录、不经复核、不递归；有意删除经复核后按 fd 递归。
>
> **Tech Stack:** Go 1.22、`golang.org/x/sys/unix` v0.25.0。
>
> **Spec:** `docs/superpowers/specs/2026-09-12-v0.7.1-openat-path-hardening-design.md`（2026-09-13 第三轮修订定向复审通过）。下文「设计 §x」均指该文档章节。

> **For agentic workers:** 每个 Task 遵循 `docs/agent-workflows/task-lifecycle.md` 的执行顺序：契约确认 → 验收矩阵 → 最小实施 → 聚焦测试 → 规格符合性审查 → 代码质量审查 → Task 专项门禁 → 计划与用户文档更新 → 主提交 → 状态封账提交 → 下一 Task。

**依据设计**：设计文档第 1–9 节。本计划只描述契约、不变量、验收测试、验证命令和提交范围，不提供生产实现代码，不规定非必要的内部 helper 名称；设计已定义、测试需要引用的三个 hook（`hookAfterOpen`、`hookBeforeMutation`、`hookAfterMkdirat`）属于测试契约，保留其名称与位置。

## 风险与编排

- 业务风险：L2。root 权限下的文件替换与删除，路径安全边界。每个 Task 完成后由未参与实施的审查者独立终审。
- 流程复杂度：单包内部重构，公开契约冻结，5 个 Task 顺序依赖。
- 并发：恢复与回滚在恢复锁租约内单线程执行，本版本不引入 goroutine、channel 或新的锁；不存在需要并发确定性测试的新逻辑。`internal/backup` 的测试 seam 与 umask 均为进程级状态，**本包测试不得使用 `t.Parallel`**（沿用 v0.7 约束）。

## 设计前置实测（2026-09-13）

以下为写计划前在 Go 1.22 容器内做的探针，用于确认设计假设未被推翻，**不是**各 Task 的验收证据：

- 环境：Docker（OrbStack）Linux 内核 `7.0.14-orbstack`，euid 0；`golang.org/x/sys/unix` v0.25.0。
- API：`Fstat`、`Fchmod`、`Fchown`、`AT_REMOVEDIR` 在 `GOOS=linux`、`GOOS=darwin` 下均可编译（关闭设计 §6.1 推迟项）。
- `Openat(O_RDONLY|O_DIRECTORY|O_NOFOLLOW)` 对指向目录的符号链接、指向文件的符号链接、普通文件、悬空符号链接**均返回 `ENOTDIR`**，未观察到 `ELOOP`。
- 打开子目录后将其 `rename` 到根外，再相对该 fd `Openat(O_CREAT|O_EXCL)`：成功，文件落在根外。
- `Fchmodat(dirfd, name, 0700, 0)` 穿过符号链接修改了根外目录权限；带 `AT_SYMLINK_NOFOLLOW` 时返回 `ENOTSUP`，根外权限不变。
- `Unlinkat(…, AT_REMOVEDIR)`：非空目录返回 `ENOTEMPTY`；指向目录的符号链接返回 `ENOTDIR`，链接目标完好。

结论与设计 §3.3、§6.3、§6.4、§6.7 一致，无需回到设计修订。**验证缺口**：该内核不是受支持的生产内核（Ubuntu 22.04 为 5.15，Ubuntu 24.04 为 6.8），errno 细节可能不同；设计 §6.4 的分类不依赖具体 errno，因此不阻塞实施，但各 Task 证据须记录实际内核版本，生产内核上的行为留待发布后真机验证。

## 按路径访问的完整盘点（2026-09-13）

以下是 `internal/backup` 当前全部按路径访问文件系统的调用，按函数归类，逐一分配可信根与 Task。实施完成后，除「归档输入」外，表中调用都须改为设计规定的 fd 相对方式；静态检查（Task 4）以本表为白名单依据。

| 函数 | 当前按路径的操作 | 可信根或输入认定 | Task |
|---|---|---|---|
| `Create` 源侧（遍历与读取） | `Lstat` 树根与显式文件、`WalkDir`、`ReadDir`、`OpenFile(O_NOFOLLOW)` | `SourceRoot`（设计 §4） | 4 |
| `Create` 输出侧（保留名、暂存、发布、目录 `fsync`） | `Lstat`/`Remove` `--out`、`MkdirAll`/`Chmod` `BackupDir`、`OpenFile(O_EXCL)`、`CreateTemp`、`Rename`、`Open` 目录 | `BackupDir` 或 `--out` 父目录（设计 §3.1） | 4 |
| `Restore` 暂存、发布、父目录创建与失败清理 | `Lstat`、`Mkdir`、`Chmod`、`CreateTemp`、`Rename`、`Remove` | `DestRoot` | 2 |
| `Restore` 预览（`DryRun`） | `Lstat`、`ReadFile` 目标 | `DestRoot`（设计 §3.1 补充表） | 2 |
| `Verify`、`ReadManifest`、`Restore` 读取归档 | `Open(archivePath)` | 调用方给出的输入文件，不属于可信根之下（设计 §3.1 补充表），保持按路径打开 | 4（静态检查白名单） |
| `Rollback` 目标替换与删除 | `Mkdir`、`Chmod`、`CreateTemp`、`Rename`、`Remove` | `DestRoot` | 2 |
| `Rollback` 读取 payload | `ReadFile(Dir/Payload)` | `RollbackSnapshot.Dir`（设计 §3.1 补充表） | 2 |
| `CaptureSnapshot` 读取目标 | `Lstat`、`OpenFile(O_NOFOLLOW)` | `DestRoot` | 3 |
| `CaptureSnapshot` 写入与失败清理 | `MkdirAll`/`Chmod` 根、`Mkdir`/`Chmod` 事务目录、暂存与发布、`RemoveAll` | `SnapshotRoot` | 3 |
| `LoadSnapshot` | `ReadFile(dir/snapshot.json)` | `dir`（设计 §3.1 补充表） | 3 |
| `CleanupSnapshot` | `RemoveAll(Dir)` | `Dir` 的父目录（设计 §3.1 补充表） | 3 |
| `PruneSnapshots` | `ReadDir(root)`、按路径 `LoadSnapshot`、`RemoveAll` | 参数 `snapshotRoot`（设计 §3.1 补充表） | 3 |

## Global Constraints（优先于各 Task，Task 不得覆盖）

1. **契约冻结**：设计 §5 列出的导出函数、结构体与字段、接口、7 个错误哨兵、全部失败原因/跳过原因/预览动作常量、归档与快照格式、权限模型、退出码与 CLI 输出一律不变。任何 Task 需要改动其中一项，停止并报告。
2. **可信根与根下解析**：可信根（`DestRoot`、`SnapshotRoot`、`BackupDir`、`--out` 的父目录）按设计 §3.1、§6.2 解析；可信根之下的名字只能按设计 §6.3 逐级 `O_NOFOLLOW` 解析。迁移完成的路径中不得残留基于完整路径的创建、替换、删除、读取、`Lstat`。
3. **禁止按名字设权限或属主**：不得使用 `Fchmodat(dirfd, name, …)`、`Fchownat(dirfd, name, …)`、`os.Chmod`、`os.Chown` 作用于可信根之下的名字（设计 §6.3）。
4. **目录 mode 与清理责任**：按设计 §6.3 表格逐行执行，无统一默认 mode；`EEXIST` 的目录项不 `Fchmod`、不进入清理列表；快照事务目录必须新建。
5. **复核与例外**：设计 §6.5 列出的变更与读取调用前执行祖先链复核；设计 §6.7 定义的失败清理不经复核、按记录删除、不递归；设计 §6.6 的有意删除经复核。
6. **表述约束**：代码注释、测试名、错误文案与文档不得把祖先链复核、身份比对表述为保证；不得声称 `Mkdirat` 成功证明后续 fd 属于本次创建（设计 §3.3、§6.3）。
7. **测试 seam**：三个 hook 按设计 §6.5 的位置实现，生产构建为空操作。引入理由：TOCTOU 类不变量依赖「在两次系统调用之间改变文件系统」，现有结构没有可控注入点，仅靠外部并发无法得到确定性结果；较小的改动（例如只在测试中并发替换）无法稳定命中窗口。行为等价性由既有测试原样通过与 §7.3 跨版本测试证明。
8. **安全测试的变异证据**：设计 §7.1 表中每条测试（T1-写、T1-删、T1-读、T2 三种操作、T3a、T3b、T4、T5、T7、T8、T9、T10）必须在对应 Task 记录变异补丁、失败的测试名与失败输出、还原后的通过输出。变异只改表中列出的那一处。本计划新增的 P1–P5 与替代测试 R1–R3 同样适用。T6 为回归项，不要求变异。
9. **既有测试**：v0.7 与 PR #7 的既有测试断言不得删弱或放宽。依据设计 §7.3「确需改写的须在实施计划中逐条说明理由」，只允许以下两类改动，每一处都须在对应 Task 的「规格偏差」中逐条列出：
   - **接线调整**：仅因内部签名变化，只改注入与调用方式，断言语句与期望值保持不变。本计划预见 7 处（Task 2 三处、Task 4 四处）。
   - **测试替换**：被测对象是设计要求删除的内部函数，原测试无法保留时，须先给出逐条断言到等价行为测试的映射，替代测试须在删除原测试之前的 Task 中落地并通过，删除时逐条核对映射。不得以覆盖范围更窄的测试替代。本计划预见 1 处：`TestResolveUnderRoot_KeepsPathsInside`（映射见 Task 1 的 R1–R3 与 Task 4）。
   - 除上述已列项外，出现其他需要改动的既有测试，停止并报告。
10. **验证环境**：Go 命令在 `golang:1.22` 容器执行（本机 `go` 不作为证据），模块缓存可挂载 `sbsm-gomod`；Linux 行为测试以容器结果为准并记录 `uname -r`；ShellCheck 使用与 CI 相同的 Ubuntu apt 版本（当前为 0.9.0）。
11. **提交与分支**：每个 Task 独立主提交与状态封账提交；未通过规格符合性与代码质量审查不得进入下一 Task。设计文档、本计划与全部实现在同一分支 `feature/v0.7.1` 完成，基线为 GitHub `main` 的 `bb5fea3`，整个 v0.7.1 经一个 PR 合并（2026-09-13 用户决定）。实施期间对设计或计划的修订、各 Task 的完成记录回填均作为该分支上的普通提交，不另开 PR；涉及设计 §5 冻结契约的修订仍须先停止并报告。仓库启用分支保护，禁止强制推送。

## 统一验证命令

以下命令在各 Task 中按名称引用：

- **聚焦**：`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 go test ./internal/backup -run '<pattern>' -count=1 -v`
- **Go 专项门禁**：`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 sh -c 'uname -r && go vet ./... && gofmt -s -l . && go test -race -count=1 ./... && GOOS=darwin go build ./...'`，`gofmt -s -l .` 必须无输出。
- **Shell 回归**：`docker run --rm -v "$PWD":/mnt -w /mnt ubuntu:24.04 sh -c 'apt-get update -qq && apt-get install -y -qq shellcheck && shellcheck install-proxy.sh merge-nodes.sh tests/test_install_json.sh tests/test_install_proxyctl.sh tests/test_install_monitor.sh tests/test_install_update.sh'`；`bash tests/test_install_update.sh && bash tests/test_install_proxyctl.sh && bash tests/test_install_json.sh && bash tests/test_install_monitor.sh`。
- **Git 门禁**：`git diff --check`、`git diff --cached --name-only`、`git status --short`、`git show --stat --oneline HEAD`。

---

## Task 1: 依赖、根下解析、错误分类、复核与 seam

**Files:**
- Modify: `go.mod`（`golang.org/x/sys` 移出 `// indirect`）
- Create: `internal/backup` 下承载本 Task 能力的实现文件与对应 `_test.go`（文件名由实施者决定，须在完成记录中列出）
- Modify: `.github/workflows/ci.yml`（增加 `GOOS=darwin go build ./...` 步骤）

**契约：**

- 可信根打开：设计 §6.2。需要创建时沿用跟随符号链接的创建方式；给定 mode 时对根 fd 执行 `Fchmod`。
- 根下逐级打开与目录创建：设计 §6.3，包括按用途显式传入 mode、「允许既有」与「必须新建」两种策略、`EEXIST` 语义、第 1–4 步顺序、`Mkdirat` 成功不证明后续 fd 归属。
- 错误分类：设计 §6.4 表格，集中于单一实现，供后续 Task 复用。
- 祖先链复核：设计 §6.5，逐层比较 `(st_dev, st_ino)`，不一致、缺失或打开失败返回 `ErrUnsafePath`。
- 测试 seam：`hookAfterOpen`、`hookBeforeMutation`、`hookAfterMkdirat`，位置与设计 §6.5 流程一致，生产为空操作。
- 本 Task **不改变**任何公开入口的行为；新能力只被本 Task 的测试直接驱动。
- 替代测试 R1–R3：为 Task 4 删除 `TestResolveUnderRoot_KeepsPathsInside` 预先落地等价的行为验收（Global Constraints 第 9 条「测试替换」）。原测试断言与替代关系：

| 原断言 | 原测试行为 | 替代测试 | 等价的行为验收 | 精确变异 | 变异后必然失败的原因 |
|---|---|---|---|---|---|
| ① `../escape` 返回 `ErrUnsafePath` | 穿越路径被拒绝 | R1 | 以「允许创建」策略驱动根下解析：逻辑路径 `../escape` 返回 `ErrUnsafePath`（原输入原样保留）；另以 `../escape/file` 驱动，返回 `ErrUnsafePath`，且可信根的父目录中不出现名为 `escape` 的目录项（`escape` 在该输入中是需要创建的父目录，使副作用可观察） | 从根下解析入口删除逻辑路径校验 | `../escape` 不再返回 `ErrUnsafePath`；`../escape/file` 经 `..` 逐级解析，在根的父目录创建 `escape` |
| ② `/etc/passwd` 返回 `ErrUnsafePath` | 绝对路径被拒绝 | R2 | 以「只解析不创建」驱动，逻辑路径 `/etc/passwd`：返回 `ErrUnsafePath`，不返回任何已打开对象 | 同上 | 返回结果不再是 `ErrUnsafePath`：视实现方式，或把绝对路径交给 `Openat`（忽略 dirfd）而解析到真实 `/etc`，或以空分量等非安全类错误失败；两种都不满足断言 |
| ③ `etc/caddy/Caddyfile` 解析为 `filepath.Join(root, "etc/caddy/Caddyfile")` | 合法路径解析到指定根之下的该位置 | R3 | 在测试根中预置 `etc/caddy/Caddyfile`；经根下解析打开该对象，其 `(st_dev, st_ino)` 等于 `os.Stat(filepath.Join(root, "etc/caddy/Caddyfile"))` 的结果 | 解析改为相对可信根的父目录（或其他错误基准）起步 | 打开的对象不是根下该位置的文件，身份不一致或解析失败 |

  fd 化后解析不再返回路径字符串，原断言 ③ 的「路径相等」无法原样保留，R3 以对象身份相等验收同一行为：合法逻辑路径解析到指定根之下的对应位置。

**不变量：**

- 根下任一分量为符号链接时不被跟随，按设计 §6.4 返回 `ErrUnsafePath`；普通文件充当父目录时返回非安全类错误。
- 只有本次 `Mkdirat` 成功的目录被 `Fchmod` 并记录；`EEXIST` 的目录项既不 `Fchmod` 也不记录。
- 「必须新建」策略下 `EEXIST` 返回错误，既有目录的权限、内容与存在性不变。
- 所有 fd 在成功、失败、取消路径上配对关闭。

### 验收矩阵

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| T4 错误分类（设计 §6.4） | 符号链接父目录在 Linux 返回 `ENOTDIR`，被误判为非安全错误，破坏 `ErrUnsafePath` 契约 | 正常目录链逐级打开成功 | 符号链接 → `ErrUnsafePath`；普通文件/FIFO → 非安全类错误；`Openat` 与 `Fstatat` 之间变为目录或消失 → `ErrUnsafePath`；其他 errno → 普通 I/O 错误 | 不适用：分类是同步系统调用序列，无阻塞点 | 不适用：只读 | Linux 实测分支（记录观察到的 errno）；`ELOOP` 分支以构造错误值驱动；竞态分支以构造的 `Fstatat` 结果驱动 | 聚焦 | 待执行 |
| T3a 设权限前以 `O_NOFOLLOW` 打开新目录 | 按名字设权限跟随符号链接，改掉根外目录权限 | 新建目录后按 fd 设权限，权限为给定 mode | `hookAfterMkdirat` 中把新目录换成指向根外的符号链接 → `ErrUnsafePath`，根外权限位不变 | 不适用：同上 | 被替换的目录项不按名字清理，错误中注明需人工确认（设计 §6.3） | 按设计 §7.1 T3a 的 hook、动作、精确变异执行 | 聚焦 | 待执行 |
| T3b 权限只作用于已打开的 fd | 复核之后、`Fchmod` 之前的替换使权限落到根外 | `Fchmod` 作用于所持 inode | `hookBeforeMutation` 中根内改名并放置符号链接 → 根外权限位不变，后续复核返回 `ErrUnsafePath` | 不适用：同上 | 不适用 | 按设计 §7.1 T3b 执行 | 聚焦 | 待执行 |
| T8 「必须新建」策略 | 复用或改写既有事务目录 | 新建目录 mode 为 `0700` | 预置同名目录（`0755`，含文件）→ 返回错误；预置目录权限、内容、存在性不变 | 不适用：同上 | 不删除预置目录 | 以私有能力直接驱动，按设计 §7.1 T8 执行 | 聚焦 | 待执行 |
| 「允许既有」策略不放宽既有目录 | 恢复时把既有目录改为 `0755` | 缺失目录新建并为给定 mode | 既有目录（`0750`，其他属主）保持原权限与属主，且不进入记录 | 不适用：同上 | 既有目录不进入清理列表 | 私有能力层面的 T7 前置测试（端到端 T7 在 Task 2） | 聚焦 | 待执行 |
| 祖先链复核（设计 §6.5） | 复核缺失或比较错误，检测能力失效 | 链未变化时通过 | 任一层被移出、替换或缺失 → `ErrUnsafePath` | 不适用：同上 | 不适用 | 私有能力层面直接驱动；端到端 T2 在 Task 2/3 | 聚焦 | 待执行 |
| T6 根下符号链接仍被拒绝 | 迁移后回归 | — | 根下符号链接父目录 → `ErrUnsafePath` | 不适用 | 不适用 | 私有能力层面回归 | 聚焦 | 待执行 |
| R1–R3 替代 `TestResolveUnderRoot_KeepsPathsInside` | 删除原测试后穿越、绝对路径拒绝或「解析到根下对应位置」失去覆盖 | R3：合法路径解析到根下对应对象 | R1、R2：`ErrUnsafePath`，且无根外副作用 | 不适用：同步系统调用序列 | R1：根的父目录无新增目录项 | R1–R3，按上表精确变异 | 聚焦 | 待执行 |
| 依赖与跨平台构建 | 引入仅 Linux 可编译的调用 | Linux 与 Darwin 均可构建 | — | 不适用 | 不适用 | `GOOS=darwin go build ./...`（本地门禁与 CI） | Go 专项门禁 | 待执行 |

**README：** 不适用（内部实现，无用户可见行为）。

## Task 完成记录

- 状态：`已封账`
- 实际范围：新增 `internal/backup/dirfd.go`（`openTrustedRoot`、`openChildDir`/`classifyDirOpenError`、`sameDirEntry`/`verifyChain`/`prepareMutation`、`resolveOneComponent`/`resolveParentDirs`/`dirChain`、`resolveUnderTrustedRoot`，及 `hookAfterOpen`/`hookBeforeMutation`/`hookAfterMkdirat` 三个测试 seam）与对应测试 `internal/backup/dirfd_test.go`；`go.mod` 将 `golang.org/x/sys` 移出 `// indirect`；`.github/workflows/ci.yml` 增加 `GOOS=darwin go build ./...` 步骤。未改动任何公开入口（`Restore`/`Rollback`/`CaptureSnapshot`/`Create` 及既有 `resolveUnderRoot` 等路径式实现原样保留），新能力只被本 Task 的测试直接驱动，符合契约。
- 规格偏差：无。
- 主提交 SHA：`dc23aef`
- 状态封账：`已提交（由 Git 历史和固定提交消息识别）`
- README：`不适用（内部实现）`
- 聚焦测试：`internal/backup` 全量（含新增 15 个测试函数：`TestOpenTrustedRoot`、`TestClassifyDirOpenError`、`TestResolveOneComponent_T3a_RejectsSymlinkSwappedInBeforeOpen`、`TestResolveOneComponent_T3b_ChmodActsOnlyOnHeldFd`、`TestResolveParentDirs_PreMkdiratVerificationCatchesAMovedAncestor`、`TestResolveOneComponent_T8_MustCreatePolicy`、`TestResolveOneComponent_AllowExistingDoesNotWidenPermissions`、`TestVerifyChain_UnchangedChainPasses`、`TestVerifyChain_CatchesADirectoryMovedOutAndReplaced`、`TestVerifyChain_CatchesAMissingDirectory`、`TestVerifyChain_CatchesADirectoryReplacedByARegularFile`、`TestResolveUnderTrustedRoot_T6_RejectsSymlinkAncestor`、`TestResolveUnderTrustedRoot_R1_RejectsRelativeTraversal`、`TestResolveUnderTrustedRoot_R2_RejectsAbsolutePath`、`TestResolveUnderTrustedRoot_R3_ResolvesToTheSameObjectUnderRoot`）：`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 go test ./internal/backup -count=1 -v`，全部通过，`uname -r` = `7.0.14-orbstack-00380-ga7e0a2dc9535`；另在 `-u 1000:1000`（非 root）下重跑 T3a/T3b/T8/AllowExisting/OpenTrustedRoot 子集确认与 umask 修复相关的断言在非 root 下同样有效且不产生 `EACCES` 误报。
- Task 专项门禁：统一验证命令「Go 专项门禁」，`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 sh -c 'uname -r && go vet ./... && gofmt -s -l . && go test -race -count=1 ./... && GOOS=darwin go build ./...'`：通过（`uname -r` = `7.0.14-orbstack-00380-ga7e0a2dc9535`；`go vet`、`gofmt -s -l .` 均无输出；`go test -race` 全部包 `ok`；`GOOS=darwin go build` 通过）。验收矩阵「依赖与跨平台构建」行由此覆盖。
- 变异证据（Global Constraints 第 8 条，T3a、T3b、T4、T6、T8、「允许既有」私有前置测试、祖先链复核私有测试、R1、R2、R3，以及本 Task 复审新增的「Mkdirat 前置复核」「verifyChain 分类」「openTrustedRoot Fchmod」三处）：均已在实施过程中执行精确变异 → 确认目标测试失败（含失败原因与输出）→ 还原 → 确认通过；证据保留在本次会话记录，未单独落盘（`internal/backup` 测试文件本身即验收产物,过程性变异补丁未提交）。T6 为回归项，不要求变异。
- 独立终审：两轮。首轮由独立上下文 Opus 子代理执行规格符合性 + 代码质量合并审查，结论 FAIL，1 项 blocking（`Mkdirat` 前缺少变更前复核，design §6.5）+ 4 项 major（`verifyChain` 对非 `ErrUnsafePath` 失败的分类过细、方向性目录 mode 断言在默认 umask 下不可证伪、`openTrustedRoot` 零测试覆盖、`hookAfterOpen` 从未被驱动）+ 3 项 minor；全部按发现修复并逐条提供变异证据。定向复审（同一 Opus 子代理续跑，覆盖本轮差异）确认前 7 项均已解决,但发现修复引入的新缺陷：`withUmask(t, 0777)` 会同时屏蔽属主位，导致新建目录在 `Fchmod` 执行前为权限 `0000`,断言所在测试自身后续的 `O_NOFOLLOW|O_DIRECTORY` 打开在非 root 下即以 `EACCES` 失败（掩盖为「needs manual confirmation after creation」）,只在门禁以 root 身份运行时不可见。改为 `withUmask(t, 0277)`（保留属主读/执行位）后,另开一次窄范围确认,验证：root 与非 root（uid 1000）下 `go test` 均通过；对 `resolveOneComponent`/`openTrustedRoot` 的 `Fchmod` 步骤分别做变异,非 root 下仍能正确捕获（`mode = -r-x------` 而非 `EACCES`）；完整门禁复跑通过；工作区在审查前后逐次核对为一致。
- 复用或省略的验证：Shell 回归本 Task 未涉及 shell 文件改动,不适用,未运行。
- 未解决风险：
  - 依赖第 6.5 节要求的三个 hook 位置正确性只在 Task 1 私有能力层面验证；端到端（`Restore`/`Rollback`/`CaptureSnapshot` 实际接线后）的 T1-写/删/读、T2、T7、T9、T10 等在 Task 2/3 落地前不构成完整证据,按计划属于后续 Task 范围,非本 Task 缺口。
  - `dirChain.created` 字段目前只写入、未被读取（供 Task 2/3 的失败清理逻辑使用）,本 Task 未新增专门测试覆盖其累积正确性（多组件场景）,留待 Task 2/3 接线后随端到端测试一并验证。
  - 生产内核（Ubuntu 22.04/24.04）上的行为未实测,沿用计划「设计前置实测」与本 Task 门禁记录的验证缺口,发布后待验证。

---

## Task 2: Restore 与 Rollback 的写入、删除、读取与失败清理

**Files:**
- Modify: `internal/backup/restore.go`、`internal/backup/snapshot.go`（`Rollback`）及对应测试文件

**契约：**

- `Restore` 暂存、发布、父目录创建按设计 §6.3（恢复行：`0755`、允许既有、清理本次新建的空目录）、§6.5（变更前复核）、§6.6（`Renameat` 发布、`Fsync(parentFd)`）。
- `Rollback` 目标替换与父目录创建按设计 §6.3（回滚行：`0755`、允许既有、不清理新建目录）；`Exists=false` 目标的删除属于有意删除，经复核后 `Unlinkat`（设计 §6.6）。
- 失败清理按设计 §6.7：适用对象、不经复核、身份比对为尽力检测、文件 `Unlinkat`、目录 `AT_REMOVEDIR`、不递归、逆序。
- `Restore` 预览（`DryRun`）按设计 §3.1 补充表读取：父目录只解析不创建；末级非普通文件记 `ActionOverwrite` 且 `OldSHA256` 为空；普通文件以 `O_NOFOLLOW` 打开读取；不执行复核；零写入、零加锁语义不变。
- `Rollback` 读取 payload 按设计 §3.1 补充表：以 `RollbackSnapshot.Dir` 为可信根；**仅对 `Exists=true` 条目**校验 payload 名为单一路径分量，相对其 fd 以 `O_NOFOLLOW` 打开并复核为普通文件，再校验既有 SHA-256；payload 名非法时该条目为 `FailReadSnapshot`，错误包装 `ErrUnsafePath`，其余条目照常处理。`Exists=false` 条目不校验、不读取 payload，保持 v0.7 语义删除目标。
- 两阶段写入顺序、取消与超时检查点位置、`Result`/`RollbackResult` 的字段语义与失败原因分类保持 v0.7（含 `FailStagedDiscarded`、`FailUnsafePath` 的使用场景）。

**不变量：**

- 设计 §3.3 无前提保证 1–4 在 `Restore` 与 `Rollback` 路径上成立。
- 普通文件充当父目录时，`Restore` 仍为 `FailStaging`、`Rollback` 仍为 `FailWrite`（设计 §5）。

**预期测试接线调整**（断言不变，须在「规格偏差」逐条记录）：

- `TestRestore_Cancellation`（between publishes 子测试）、`TestRestore_Timeout`、`TestRestore_LockReleasedOnPanic` 通过 `publishFileFn` 以路径签名 `func(tmp, dst string) error` 注入。发布改为 fd 相对后，若该 seam 签名随之变化，只调整这三个测试的注入代码；注入时点（两次发布之间）与全部断言保持不变。

### 验收矩阵

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| T1-写 fd 相对的暂存创建与发布 | 符号链接替换使恢复写到根外 | 无替换时恢复成功，字节、mode、属主与清单一致（既有测试） | 设计 §7.1 T1-写：返回 `ErrUnsafePath`（`FailUnsafePath`），根外目录为空 | 沿用 v0.7：取消与超时检查点不变，由 `TestRestore_Cancellation`、`TestRestore_Timeout` 回归 | 暂存文件按失败清理删除 | T1-写，按设计 §7.1 精确变异 | 聚焦 | 待执行 |
| T1-删 fd 相对的删除 | 回滚删除作用于根外同名文件 | `Exists=false` 目标被删除（既有测试） | 设计 §7.1 T1-删：根外 `Caddyfile` 仍在 | 沿用 v0.7：`TestRollback_Cancellation`、`TestRollback_Timeout` 回归 | 不适用：有意删除不留临时产物 | T1-删 | 聚焦 | 待执行 |
| T2 复核能发现打开之后、复核之前的移动（写、删） | 目录被移出根后写入或删除落在根外 | 链未变化时正常完成 | 设计 §7.1 T2：写入与删除均返回 `ErrUnsafePath`，根外无新增、无删除 | 同上 | 本次暂存产物按失败清理删除 | T2-写、T2-删；测试名须体现「复核之前且复核时可观察」 | 聚焦 | 待执行 |
| P1 预览不跟随末级符号链接 | 预览读取根外文件并把摘要显示为「unchanged」，误导操作员 | 普通文件预览摘要与动作正确（既有 `TestRestore_DryRunNoWrites`） | P1a（回归，不要求变异）：静态布局下 `DestRoot/etc/caddy/Caddyfile` 为指向根外文件的符号链接 → `ActionOverwrite`、`OldSHA256` 为空；父目录为符号链接 → `ErrUnsafePath`。P1b：`hookBeforeMutation` 位于末级 `Fstatat` 已判定为普通文件之后、读取的 `Openat` 之前，把该文件换成指向根外文件的符号链接，根外文件字节与归档一致 → 打开失败并按非普通文件处理，`ActionOverwrite`、`OldSHA256` 为空 | 沿用 v0.7：预览取消由既有测试覆盖 | 零写入、零加锁（既有 `TestRestore_DryRunNoLock`） | P1b；精确变异：末级读取改为按路径 `os.ReadFile`，`Fstatat` 判定保留；变异后跟随符号链接读到与归档一致的字节，记为 `ActionUnchanged` 且带摘要。P1a 静态布局同时受 `Fstatat` 判定与 `O_NOFOLLOW` 读取两重防御，单点变异无法使其失败，故只作回归 | 聚焦 | 待执行 |
| P2 回滚读取 payload 不跟随符号链接 | 回滚把根外文件内容写入 `DestRoot` 目标 | 正常快照回滚逐字节还原（既有测试） | 以 `CaptureSnapshot` 生成快照后，把某条目 payload 换成指向根外文件的符号链接，并把内存中快照值该条目的 SHA-256 设为根外文件的摘要 → 该条目 `FailReadSnapshot`，目标内容不变 | 沿用 v0.7：`TestRollback_Cancellation`、`TestRollback_Timeout` | 快照保留（既有语义） | P2；精确变异：payload 改为按路径 `os.ReadFile(Dir/Payload)`；变异后校验通过并把根外字节写入目标 | 聚焦 | 待执行 |
| P3-回滚 payload 名校验仅作用于 `Exists=true` | ① `..` 使 fd 相对读取逃出事务目录；② 校验误作用于 `Exists=false` 条目，使回滚无法删除本次新建的目标 | 混合条目快照正常回滚：`Exists=true` 条目逐字节还原，`Exists=false` 条目（`Payload` 为空）的目标被删除（既有 `TestRollback_RestoresBytesModesOwnershipAndDeletesCreated`） | 混合快照值：条目 A 为 `Exists=false`、`Payload` 为空，目标存在；条目 B 为 `Exists=true`、`Payload` 为 `../outside`，事务目录的父目录放置对应文件且 SHA-256 匹配 → A 进入 `Deleted`、其目标被删除；B 为 `FailReadSnapshot`、错误包装 `ErrUnsafePath`，B 的目标内容不变 | 不适用：同步 | 不适用 | P3-回滚；两个精确变异分别验证：(a) 删除 `Exists=true` 条目的 payload 名校验 → `Openat` 解析 `..` 读到根外文件并写入 B 的目标；(b) 把校验扩展到全部条目 → A 因空 payload 被判非法，未进入 `Deleted`、目标残留 | 聚焦 | 待执行 |
| T7 既有目录不被放宽权限（端到端） | 恢复把既有目录改为 `0755` | 新建父目录为 `0755` | 设计 §7.1 T7：既有 `etc/caddy` 保持 `0750` 与原属主 | 不适用：静态布局 | 既有目录不进入清理 | T7 | 聚焦 | 待执行 |
| T9 失败清理不经复核 | 复核失败导致暂存文件残留 | — | 设计 §7.1 T9：`etc/caddy.moved` 中不残留暂存文件 | 不适用 | 暂存文件被删除 | T9 | 聚焦 | 待执行 |
| T10 真实目录替换下仍成立的结构性保证 | 失败清理递归删除替换目录内容 | — | 设计 §7.1 T10：`token` 为 `FailStaging`、`Caddyfile` 为 `FailStagedDiscarded`；根外无变化；`keep` 与替换目录仍在；不断言替换目录的权限位 | 不适用 | 失败清理只删本次暂存文件；`AT_REMOVEDIR` 对非空目录失败并报告 | T10 | 聚焦 | 待执行 |
| v0.7 恢复与回滚回归 | 迁移改变既有行为 | `internal/backup` 与 `cmd/proxyctl` 中既有 `Restore`/`Rollback`/`CLI restore` 测试全部通过 | 同左 | 同左 | 同左 | 既有测试（接线调整见上） | Go 专项门禁 | 待执行 |

**README：** 不适用（内部实现，无用户可见行为）。

## Task 完成记录

- 状态：`已封账`
- 实际范围：`internal/backup/restore.go`：`Restore` 的暂存创建（新增 `createStagingFile`/`writeStagingFile`/`randomTmpName`，返回值携带已打开的暂存文件 fd）与发布（`publishFileFn`/`publishStagedFile`，签名 `func(chain *dirChain, tmp, dest string) error`）改为 fd 相对；失败清理改为 `createdDirRef`/`removeCreatedDirsFd`/`removeStagedFileFd`，均先经 `identityMatches`（设计 §6.7 身份比对）再执行、不一致则跳过并记入 `res.Warnings`；`previewRestore`/`previewOneFile` 改为 fd 相对只解析、不创建、不复核，`hookBeforeMutation` 仅作为 P1b 的裸注入点；`Restore` 发布失败与写入失败按 `errors.Is(err, ErrUnsafePath)` 区分 `FailUnsafePath` 与原有的 `FailPublish`/`FailStaging`。`internal/backup/snapshot.go`：`Rollback` 目标写入侧改为「先读取并校验 payload、再解析/创建目的地目录链」+ 新增 `publishNewFile`（复用 `restore.go` 的 `createStagingFile`/`publishFileFn`，返回值携带 §6.7 警告文本供 `Rollback` 折入返回的 `error`）；删除侧改为新增 `deleteRollbackTarget`（fd 相对复核后 `Unlinkat`+`Fsync`，`ENOENT` 视为已删除、`ErrUnsafePath` 归类为 `FailUnsafePath`、其余归 `FailDelete`；缺失中间父目录归 `FailDelete`，与 v0.7 一致，不再视为「已删除」成功）；payload 读取侧新增 `validatePayloadName`/`readRollbackPayload`，仅对 `Exists=true` 条目生效。`internal/backup/dirfd.go`：修正 `resolveParentDirs`/`resolveUnderTrustedRoot` 的错误路径契约（见规格偏差 1）。`CaptureSnapshot`/`LoadSnapshot`/`PruneSnapshots`/`stageFile`/`writeFileAtomic`/`publishFile`/`resolveUnderRoot`/`ensureParentDir`/`checkParentChain`/`removeCreatedDirs` 未改动，留给 Task 3。
- 规格偏差：
  1. **对 Task 1 已封账的 `dirfd.go` 的必要修正**：`resolveParentDirs`/`resolveUnderTrustedRoot` 原实现在链中途解析失败时关闭全部已开 fd 并返回 `nil`，导致调用方无法得知本次调用途中新建了哪些目录（`chain.created` 信息随之丢失），使 `Restore` 无法履行「失败清理逆序删除本次新建的空目录」这一 Task 2 自身契约。改为：解析失败时返回**仍处于打开状态的部分 chain**（`chain.created` 完整保留），由调用方决定清理与关闭；仅路径字符串校验失败（`ValidateLogicalPath`）时才返回 `nil`。已重跑 Task 1 全部测试确认无回归；`dirfd_test.go` 中两处直接调用 `resolveParentDirs`/`resolveUnderTrustedRoot` 且在错误路径丢弃返回值的测试（`TestResolveParentDirs_PreMkdiratVerificationCatchesAMovedAncestor`、`TestResolveUnderTrustedRoot_T6_RejectsSymlinkAncestor`）补充了 `chain.closeOpened()`，断言不变。此项超出计划预列的「3 处接线调整」范围，按生命周期文档的「验证与验收中的修改边界」条款处理：为已批准需求（Task 2 自身的失败清理契约）修复可证实的具体缺陷，非顺手加入功能，已在独立终审中核实。
  2. **`Openat(...O_CREAT...)` 补齐变更前复核**：设计 §6.5 明确列出 `Openat(...O_CREAT...)` 属于需要复核的变更系统调用；Task 1 交付时遗漏了这一步（只有 `Mkdirat` 与 `Fchmod` 前有复核）。已在 `Restore` 的暂存创建与 `publishNewFile`（Rollback 写入）中各补一次 `prepareMutation`，随附新增测试与变异证据（见下）。
  3. **发布/写入失败的错误分类**：v0.7 的 `FailPublish`（Restore）与 `FailWrite`（Rollback）原本是发布失败的唯一归类；本 Task 按设计 §5 的统一规则新增 `errors.Is(err, ErrUnsafePath)` 判定，命中时改报 `FailUnsafePath`。`TestRestore_RenameFailureReporting`（非安全类的真实 rename 冲突）验证仍归 `FailPublish`，未受影响。
  4. 预期测试接线调整（计划已列，3 处）：`TestRestore_Cancellation`(between publishes 子测试)、`TestRestore_Timeout`、`TestRestore_LockReleasedOnPanic` 的 `publishFileFn` 注入改为新签名 `func(chain *dirChain, tmp, dst string) error`，改用 `publishStagedFile` 而非 `publishFile`；注入时点与断言均不变。
  5. **独立终审发现后新增的修正**（详见下方"独立终审"记录）：(a) `Restore` 三处失败分支原先在调用 `abandonStaging` 前先 `chain.closeOpened()`，导致该 chain 自己新建目录的失败清理作用于已关闭的 fd 而静默失效——改为先 `abandonStaging`（内部完成 `removeCreatedDirsFd`）再关闭 chain；(b) 补齐设计 §6.7 强制的删除前身份比对（`identityMatches`），此前完全未实现——`createStagingFile` 改为保留已打开的 fd 供比对，`createdDirRef` 增加 `self` 字段，新增 `removeStagedFileFd`，不一致时跳过删除并产生警告（`Restore` 经 `res.Warnings`；`Rollback` 因 `RollbackResult` 无 Warnings 字段（契约冻结）而折入返回的 `error`）；(c) `Rollback` 写入侧改为先读取校验 payload、再解析/创建目的地目录链，避免一个被拒绝的 payload 在 `DestRoot` 下留下 `Rollback` 从不清理的新建目录；(d) 修正 3 处代码质量问题：`Rollback` 删除侧「缺失中间父目录」原被当作成功（`res.Deleted`），与 v0.7 实际行为（`FailDelete`，因 `syncDir(parent)` 在父目录缺失时失败）不符，已改回 `FailDelete`；`previewRestore` 上方遗留的重复旧注释已删除；`DestRoot` 不可打开时原先把全部文件标记为 `FailStaging`，改为首个 `FailStaging`、其余 `FailNotAttempted`，对齐 v0.7。
- 主提交 SHA：`58fc380`
- 状态封账：`已提交（由 Git 历史和固定提交消息识别）`
- README：`不适用（内部实现）`
- 聚焦测试：`internal/backup` 全量（含本 Task 新增 16 个测试：Task 2 验收矩阵对应的 11 个——`TestRestore_T1Write_SucceedsUnderTheRenamedDirectory`、`TestRestore_T2Write_AncestorReverificationCatchesAMoveOutOfRoot`、`TestRestore_P1a_PreviewSymlinkRegression`、`TestRestore_P1b_PreviewDoesNotFollowSymlinkSwappedInBeforeRead`、`TestRestore_T7_ExistingDirectoryPermissionsNotWidened`、`TestRestore_T9_FailureCleanupSkipsReverification`、`TestRestore_T10_RealDirectoryReplacementStructuralGuarantee`、`TestRollback_T1Delete_SucceedsUnderTheRenamedDirectory`、`TestRollback_T2Delete_AncestorReverificationCatchesAMoveOutOfRoot`、`TestRollback_P2_PayloadReadDoesNotFollowSymlink`、`TestRollback_P3_PayloadNameValidationOnlyAppliesToExistsTrue`；首轮独立终审发现后新增的 4 个回归测试——`TestRestore_FailureCleanupRemovesADirectoryCreatedByTheSameFailingChain`（blocking 修复）、`TestRestore_FailureCleanupSkipsADirectoryReplacedByADifferentEmptyOne`（major 修复，§6.7 身份比对）、`TestRollback_RejectedPayloadCreatesNothingUnderDestRoot`（major 修复）、`TestRollback_DeleteWithMissingParentIsFailDeleteNotSuccess`（质量修正）；第二轮定向复审发现后新增的 1 个——`TestRestore_FailureCleanupSkipsAStagedFileReplacedByADifferentFile`（§6.7 身份比对，暂存文件半边）：`docker run --rm -v "$PWD":/w -v sbsm-gomod:/go/pkg/mod -w /w golang:1.22 go test ./internal/backup -count=1 -v`，全部通过，`uname -r` = `7.0.14-orbstack-00380-ga7e0a2dc9535`。v0.7 既有 `internal/backup`/`cmd/proxyctl` 测试全部原样通过（接线调整见上，断言未改）。
- Task 专项门禁：统一验证命令「Go 专项门禁」：`uname -r` = `7.0.14-orbstack-00380-ga7e0a2dc9535`；`go vet ./...`、`gofmt -s -l .` 均无输出；`go test -race -count=1 ./...` 全部包 `ok`；`GOOS=darwin go build ./...` 通过。
- 变异证据（Global Constraints 第 8 条）：
  - Task 2 验收矩阵（T1-写、T2-写、P1b、T7、T9、T10、T1-删、T2-删、P2、P3-回滚(a)、P3-回滚(b) 共 11 处，P1a 为回归项不要求变异），每处均以 `cp` 建立独立于 git 的检查点、施加设计 §7.1 指定的精确变异、确认目标测试以设计预期的原因失败、还原后重跑该测试与全量门禁确认恢复通过：
    - T1-写：暂存改为按路径重新打开父目录 → 根外目录出现暂存文件（`os.OpenFile`/`os.Rename` 语义复现）。
    - T2-写：`verifyChain` 恒返回 `nil` → 写入不再返回 `ErrUnsafePath`（Rollback 侧 T2-删复用同一变异同时验证）。
    - P1b：末级读取改为 `os.ReadFile` 按路径 → 预览把符号链接目标误报为 `ActionUnchanged`。
    - T7：对 `Mkdirat` 返回 `EEXIST` 的目录也执行 `Fchmod` → 既有目录权限被放宽为 0755。
    - T9：失败清理前也执行复核并在复核失败时跳过 → 暂存文件残留在被替换目录下。
    - T10：失败清理改为按 fd 枚举全部内容后删除 → 换入目录中不属于本次运行的文件 `keep` 被删除。
    - T1-删：删除改为按路径 `os.Remove` → 根外文件被误删，根内真实文件保留。
    - P2：payload 读取改为按路径 `os.ReadFile` → 校验和匹配后写入了根外字节。
    - P3-回滚(a)：删除 `Exists=true` 条目的 payload 名校验 → 通过 `../outside` 读到根外字节并写入目标。
    - P3-回滚(b)：payload 名校验扩展到 `Exists=false` 条目 → 该条目因空 payload 被判非法，未进入 `Deleted`、目标残留。
  - 独立终审发现后新增的 4 处，同样以变异确认目标测试失败、还原后通过：
    - blocking 修复（a）：`abandonStaging` 前恢复 `chain.closeOpened()` → 本链自己新建的目录清理失败，`TestRestore_FailureCleanupRemovesADirectoryCreatedByTheSameFailingChain` 报告目录未被清理。
    - major 修复（b）：`removeCreatedDirsFd` 删除身份比对 → `TestRestore_FailureCleanupSkipsADirectoryReplacedByADifferentEmptyOne` 报告替换目录被误删。
    - major 修复（c）：`Rollback` 恢复「先解析目的地链、后读取 payload」旧序 → `TestRollback_RejectedPayloadCreatesNothingUnderDestRoot` 报告 `DestRoot` 下出现新建目录。
    - 质量修正：`Rollback` 删除侧缺失中间父目录恢复为「视为已删除」→ `TestRollback_DeleteWithMissingParentIsFailDeleteNotSuccess` 报告结果为成功而非 `FailDelete`。
- 复用或省略的验证：Shell 回归本 Task 未涉及 shell 文件改动，不适用，未运行。
- 独立终审：两轮，均由独立上下文 Opus 子代理执行，合并规格符合性与代码质量审查（过程中共三次遭遇速率限制中断，均在确认工作区无中间态后续跑完成）。
  - **首轮**（完整审查）：结论 FAIL：1 项 blocking（`Restore` 失败清理在 `abandonStaging` 前先关闭当前 chain，导致本链自己新建的目录清理失败）+ 2 项 major（设计 §6.7 强制的删除前身份比对完全未实现；`Rollback` 写入侧在校验 payload 前就已解析/创建目的地目录，一个被拒绝的 payload 会在 `DestRoot` 下留下从不清理的新建目录）+ 9 项 minor。全部按发现修复，新增 4 个回归测试并逐条提供变异证据；2 项 minor（`removeCreatedDirsFd` 注释的"忽略即报告"措辞、`writeStagingFile` 因保留 fd 而变化的关闭语义注释）随 major 修复顺带修正；1 项 minor（`abandonStaging` 的暂存文件清理与发布循环共享同一 `removeStagedFileFd`，该函数已通过 T9 变异证据验证不执行复核）记录为设计判断；1 项 minor（`createStagingFile` 的 `O_EXCL` 重试分支缺少独立复核）记录为实际不可达（96 位随机名冲突）并在注释中注明——该分支后续随第二轮 minor 修复一并补上身份比对（见下）；1 项 minor（`createdDirRef` 与 `dirChain` 字段重复）留待后续视情况重构。
  - **第二轮**（针对首轮 3 项修复的定向复审）：结论 PASS，可提交；额外发现 3 项 minor：(1) `identityMatches` 把「目标已不存在」（如发布已成功、仅后续 `Fsync` 失败）与「目标被替换」同等对待，误报「需人工确认」；(2) `createStagingFile` 自身失败清理路径（`writeStagingFile` 失败分支）未接入身份比对、且先关闭 fd 后删除，与「`removeStagedFileFd` 用于所有暂存文件清理点」的说法不符，在非 root 运行下可实际触达（`chown` 失败）；(3) `removeStagedFileFd` 的身份比对本身缺少专门测试（此前只有目录半边有测试）。三项均已修复：`identityMatches` 拆为 `identityCheck(parent, name, self) (match, gone bool)`，`gone`（`ENOENT`）不再产生警告；`createStagingFile` 的失败清理改为调用 `removeStagedFileFd`；新增 `TestRestore_FailureCleanupSkipsAStagedFileReplacedByADifferentFile` 并以变异（删除身份比对）确认捕获。未再发起第三轮审查：三处均为独立可验证的小改动，已通过全量门禁与新增测试的变异证据自证；符合用户 2026-09-14 确认的审查流程（每个 Task 一次独立终审覆盖规格与质量，发现修复后仅做定向复审，不代表每一处后续小修都需要新一轮审查）。
- 未解决风险：
  - `Restore` 的失败清理（`removeCreatedDirsFd`/`removeStagedFileFd`）与快照事务目录的失败清理（Task 3 `CaptureSnapshot`）共享同一设计模式（含 §6.7 身份比对）但当前各自独立实现（前者在 `restore.go`，后者留给 Task 3 新增），尚未复用同一 helper；不影响正确性，留待后续 Task 视情况收敛。
  - `createdDirRef` 复刻了 `dirChain` 已持有的字段（`fds[idx]`、`comps[idx]`、`fds[idx+1]`），可收敛为 `dirChain` 的方法以消除重复、结构性杜绝本轮 blocking 发现的那类排序错误；本 Task 未做此重构，标记为后续可选的代码质量改进。
  - 生产内核（Ubuntu 22.04/24.04）上的行为未实测，沿用计划「设计前置实测」与已记录的验证缺口，发布后待验证。

---

## Task 3: CaptureSnapshot、LoadSnapshot、CleanupSnapshot 与 PruneSnapshots

**Files:**
- Modify: `internal/backup/snapshot.go` 及对应测试文件

**契约：**

- 读取侧：`CaptureSnapshot` 在 `DestRoot` 之下按 fd 读取目标并复核类型与 `Nlink`（设计 §6.6），读取前执行复核（设计 §6.5）。
- 写入侧：`SnapshotRoot` 为可信根（`0700`，设计 §6.2）；事务目录按「必须新建」、`0700`（设计 §6.3）；payload 与 `snapshot.json` 相对事务目录 fd 创建与发布。
- 失败清理：`CaptureSnapshot` 失败时按记录删除本次写入的 payload、元数据与暂存文件，再对事务目录 `AT_REMOVEDIR`，不递归（设计 §6.7）。
- 有意删除：`CleanupSnapshot`、`PruneSnapshots` 经复核后按 fd 递归删除事务目录，不跟随符号链接，结果与 v0.7 `RemoveAll` 一致（设计 §6.6）；`PruneSnapshots` 保留无法解析的目录的 v0.7 行为不变。
- `LoadSnapshot(dir)`：`dir` 为可信根，`snapshot.json` 相对其 fd 以 `O_NOFOLLOW` 打开并复核为普通文件；除既有的 `entry.Path` 校验外，**仅对 `Exists=true` 条目**新增 payload 名单一路径分量校验，非法返回 `ErrUnsafePath`；`Exists=false` 条目的 payload 不校验（设计 §3.1 补充表）。
- `CleanupSnapshot(snapshot)`：以 `snapshot.Dir` 的父目录为可信根，事务目录名以 `O_NOFOLLOW|O_DIRECTORY` 打开（设计 §3.1 补充表）。
- `PruneSnapshots(snapshotRoot, …)`：以参数为可信根枚举事务目录，元数据相对事务目录 fd 读取，不经按路径的 `LoadSnapshot`。

**不变量：**

- 映像不记录可信根之外的字节（在设计 §3.3 保证与检测能力范围内）。
- 快照目录结构、`snapshot.json` 格式、事务 ID 格式不变。

### 验收矩阵

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| T1-读 fd 相对读取 | 映像记录根外文件内容，回滚时写回根内 | 无替换时映像字节、mode、属主正确（既有测试） | 设计 §7.1 T1-读：映像不含 `OUTSIDE` | 沿用 v0.7：`TestCaptureSnapshot_Cancellation` 回归 | 不适用 | T1-读 | 聚焦 | 待执行 |
| T2 复核能发现打开之后、复核之前的移动（读） | 读取已移出根的目录 | 链未变化时正常完成 | 设计 §7.1 T2 读取场景：返回 `ErrUnsafePath`，未生成含 `OUTSIDE` 的映像 | 同上 | 本次写入的事务目录按失败清理删除 | T2-读 | 聚焦 | 待执行 |
| 事务目录必须新建且为 `0700`（端到端） | 复用既有目录或权限过宽 | 新建事务目录 `0700`、文件 `0600`（`TestCaptureSnapshot_RecordsExistingAndMissingPaths`） | 事务 ID 冲突时返回错误，不改动既有目录 | 不适用 | 不删除既有目录 | 既有测试 + Task 1 的 T8 | 聚焦 | 待执行 |
| 快照失败清理只删记录条目 | 失败清理递归删除或误删未记录内容 | — | 事务目录中存在未记录条目时，记录条目被删除、目录保留并报告 | 取消路径：沿用 v0.7 检查点，取消后事务目录被清理（既有测试） | 按记录删除后 `AT_REMOVEDIR` | 新增：以 `hookBeforeMutation` 在事务目录中放入未记录文件后触发失败 | 聚焦 | 待执行 |
| 有意删除保持 v0.7 结果 | 递归删除改为非递归后遗留目录，或递归时跟随符号链接 | `CleanupSnapshot`、`PruneSnapshots` 删除整个事务目录（既有测试） | 事务目录内含指向根外的符号链接：删除链接本身，根外目标完好 | 不适用：同步删除 | 保留无法解析目录与最新映像的规则不变（既有测试） | 既有测试 + 新增符号链接子项测试 | 聚焦 | 待执行 |
| P3-加载 payload 名校验仅作用于 `Exists=true` | ① 被篡改的元数据通过加载，后续回滚逃逸；② 校验误作用于 `Exists=false` 条目，使含缺失文件的正常快照无法加载 | 混合条目快照加载成功：`Exists=true` 条目 `Payload` 为 `payload-0000.bin`，`Exists=false` 条目 `Payload` 为空（该形态由 `CaptureSnapshot` 产出，既有 `TestCaptureSnapshot_RecordsExistingAndMissingPaths` 同样覆盖；P3-加载本身也断言） | 同一混合形态中把 `Exists=true` 条目的 `Payload` 分别改为 `../outside`、空字符串、`a/b` → 均返回 `ErrUnsafePath` | 不适用：同步 | 不适用 | P3-加载；两个精确变异分别验证：(a) 删除 `Exists=true` 条目的校验 → 非法名加载成功；(b) 校验扩展到全部条目 → 正常混合快照加载失败 | 聚焦 | 待执行 |
| P4 元数据读取不跟随符号链接 | 加载或清理判断依据根外伪造的元数据 | 正常加载与清理（既有测试） | ① 事务目录中 `snapshot.json` 为指向根外合法快照 JSON 的符号链接 → `LoadSnapshot` 返回错误；② 同一布局下 `PruneSnapshots`：根外 JSON 的 `created_at` 设为超过保留期，并另置一个更新的正常快照，使被测目录不是最新（否则「始终保留最新」规则会掩盖变异）；该目录按「无法解析」保留（v0.7 语义） | 不适用：同步 | ② 被测目录与更新的正常快照都未被删除 | P4；精确变异：元数据改为按路径 `os.ReadFile`；变异后①加载成功、②被测目录因根外 `created_at` 过期且不是最新而被删除 | 聚焦 | 待执行 |
| P5 `CleanupSnapshot` 以父目录为可信根 | 跟随 `snapshot.Dir` 符号链接递归删除根外内容 | 正常清理（既有 `TestCleanupSnapshot`） | 把 `snapshot.Dir` 目录项换成指向根外目录（含文件 `keep`）的符号链接 → 返回 `ErrUnsafePath`；根外目录与 `keep` 仍在 | 不适用：同步 | 不删除根外内容 | P5；精确变异：把 `snapshot.Dir` 本身当作可信根按路径打开（跟随符号链接）后递归删除其内容；变异后 `keep` 被删除 | 聚焦 | 待执行 |
| v0.7 快照回归 | 迁移改变既有行为 | 全部既有快照、回滚、清理测试通过 | 同左 | 同左 | 同左 | 既有测试 | Go 专项门禁 | 待执行 |

**README：** 不适用（内部实现，无用户可见行为）。

## Task 完成记录

- 状态：`待实施 / 实施中 / 待审查 / 已封账`
- 实际范围：
- 规格偏差：`无 / 说明`
- 主提交 SHA：
- 状态封账：`待提交 / 已提交（由 Git 历史和固定提交消息识别）`
- README：`不适用（内部实现）`
- 聚焦测试：
- Task 专项门禁：
- 复用或省略的验证：
- 未解决风险：

---

## Task 4: Create 源侧与输出侧、移除路径式防御、静态检查与兼容

**Files:**
- Modify: `internal/backup/archive.go`、`internal/backup/restore.go`（删除路径式防御）及对应测试文件
- Create: 静态检查测试（位于 `internal/backup`，文件名由实施者决定）
- Create: v0.7.0 归档夹具（`internal/backup/testdata/` 下，由 v0.7.0 版本代码生成，生成方式记录在完成记录中）

**契约：**

- 源侧：`Create` 的递归树按目录 fd 递归（设计 §6.6），跳过规则与稳定原因不变；源文件读取按 fd 并复核类型与 `Nlink`。
- 输出侧：`BackupDir` 与 `--out` 父目录为可信根（设计 §3.1、§6.2）；`O_EXCL` 保留名、暂存、发布、失败清理相对输出目录 fd（设计 §6.6、§6.7）；`--out` 覆盖既有文件时不删除调用方文件的语义不变。
- 移除 `resolveUnderRoot`、`checkParentChain`、`ensureParentDir` 中基于路径的防御（设计 §6.6）；`ValidateLogicalPath` 保留。
- 静态检查按设计 §7.2，本 Task 固化白名单：仅允许打开可信根所需的调用（设计 §6.2 与 §3.1 补充表），以及归档输入路径的按路径打开（设计 §3.1 补充表认定其不属于可信根之下）。白名单须逐项对应「按路径访问的完整盘点」表。
- 跨版本兼容按设计 §7.3。

**不变量：**

- 设计 §3.1 可信根自身或其祖先为符号链接时行为与 v0.7 一致。
- 迁移完成后，`internal/backup` 内除白名单外不存在作用于可信根之下名字的路径式调用。

**预期测试替换**（Global Constraints 第 9 条「测试替换」，须在「规格偏差」逐条记录）：

- 删除 `TestResolveUnderRoot_KeepsPathsInside`（被测函数 `resolveUnderRoot` 按设计 §6.6 删除）。删除前确认 Task 1 的 R1–R3 已落地并通过，并在记录中逐条核对：断言① → R1，断言② → R2，断言③ → R3。

**预期测试接线调整**（断言不变，须在「规格偏差」逐条记录）：

- `archive_test.go` 中 `TestCreate_FailedArchiveWriteRemovesReservedName`、`TestWriteArchive_CancellationRemovesStagingFileAndKeepsReservation`、`TestCreate_OutFailureKeepsAPreExistingFile`、`TestCreate_OutFailureRemovesAFileThisRunCreated` 直接使用 `writeArchiveFn`、`collect`、`reserveDestination`、`writeArchive`。输出侧改为 fd 相对后若这些内部签名变化，只调整注入与调用代码，断言与期望值不变。

### 验收矩阵

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| 源侧遍历与读取 fd 化 | 递归树跟随替换进来的符号链接读取根外内容 | 既有 `TestCreate_*` 源侧测试通过，跳过原因不变 | 递归树内符号链接与特殊文件仍按 v0.7 跳过并记录原因；T4 覆盖 `Create` 源侧分类 | 沿用 v0.7：`TestCreate_Cancellation`、`TestCreate_DeadlineExceeded` 回归 | 不适用：只读 | 既有测试 + T4 的 `Create` 分支 | 聚焦 | 待执行 |
| 输出侧 fd 化 | 失败清理误删调用方文件，或暂存/保留名残留 | 归档原子发布，`0600`，目录 `0700` | 写入失败时保留名与暂存文件被删除；`--out` 覆盖既有文件失败时调用方文件保留 | 取消时暂存文件被清理（既有测试） | 按设计 §6.7 失败清理 | 既有 `archive_test.go` 失败与取消测试（接线调整见上） | 聚焦 | 待执行 |
| T5 可信根兼容 | 可信根改为 `O_NOFOLLOW` 造成用户可见变化 | 设计 §7.1 T5 四种符号链接可信根行为与 v0.7 一致 | — | 不适用：静态布局 | 产物权限符合设计 §5 | T5，按设计 §7.1 精确变异 | 聚焦 | 待执行 |
| 移除路径式防御 | 残留第二套实现导致漂移；删除原测试后覆盖变窄 | 相关函数移除后全部测试通过 | — | 不适用 | 不适用 | 删除 `TestResolveUnderRoot_KeepsPathsInside` 前核对 R1–R3 已通过，并逐条记录断言映射 | Go 专项门禁 | 待执行 |
| 静态检查（补充） | 新代码绕过 fd 路径 | 白名单外无禁用调用 | 在非白名单位置加入 `os.Rename` 时检查失败（变异证据） | 不适用 | 不适用 | 静态检查测试 | 聚焦 | 待执行 |
| 跨版本归档兼容 | 迁移破坏旧归档读取或恢复 | v0.7.0 归档夹具 `Verify`、`ReadManifest`、`Restore` 结果与 v0.7 一致 | — | 不适用 | 不适用 | 夹具测试 | 聚焦 | 待执行 |

**README：** 不适用（内部实现，无用户可见行为）。

## Task 完成记录

- 状态：`待实施 / 实施中 / 待审查 / 已封账`
- 实际范围：
- 规格偏差：`无 / 说明`
- 主提交 SHA：
- 状态封账：`待提交 / 已提交（由 Git 历史和固定提交消息识别）`
- README：`不适用（内部实现）`
- 聚焦测试：
- Task 专项门禁：
- 复用或省略的验证：
- 未解决风险：

---

## Task 5: 发布收尾与风险记录

**Files:**
- Modify: `install-proxy.sh`、`merge-nodes.sh`（`PROXYCTL_VERSION` → `v0.7.1`）、`tests/test_install_monitor.sh`（两处版本断言）
- Modify: `SECURITY.md`（写明设计 §3.3 的安全前提）
- Modify: `docs/superpowers/plans/2026-08-17-v0.7-backup-upgrade-rollback.md`（Task 2 TOCTOU 残留风险条目）

**契约：**

- 版本号收尾沿用 v0.6/v0.7 做法：在特性分支内升到 `v0.7.1`，`tag` 与 Release 资产在合并之后发布；资产确认可下载前不得把特性分支脚本作为安装入口。
- `SECURITY.md` 写明设计 §3.3 的安全前提原文含义：从可信根到目标文件之间的目录不得被不可信主体重命名、移动、替换或写入目录项；并说明前提被破坏时本版本不提供 A、B、C 三项保证。
- v0.7 实施计划 Task 2 的 TOCTOU 残留风险更新为设计 §8 规定的表述：符号链接替换已结构性闭环；跨根移动在安全前提下成立，复核仅缩小窗口；不得表述为完全闭环。同时保留原条目文字，新增更新说明并注明依据的 v0.7.1 提交。

### 验收矩阵

| 需求或不变量 | 主要风险 | 成功路径 | 失败路径 | 取消/超时 | 持久化/清理 | 确定性测试 | 验证命令 | 证据 |
|---|---|---|---|---|---|---|---|---|
| 版本号收尾 | 全新安装装回 v0.7.0，或断言未同步导致测试失败 | 两个脚本与测试断言均为 `v0.7.1` | — | 不适用 | 不适用 | `tests/test_install_monitor.sh` 版本断言 | Shell 回归 | 待执行 |
| 安全前提文档化 | 运维不知前提，误以为无条件保证 | `SECURITY.md` 含安全前提与前提被破坏时的保证边界 | — | 不适用 | 不适用 | 文档审查（逐项对照设计 §3.3） | Git 门禁 | 待执行 |
| 残留风险表述 | 过度声称 TOCTOU 已完全闭环 | v0.7 计划 Task 2 条目按设计 §8 更新，原文保留 | — | 不适用 | 不适用 | 文档审查 | Git 门禁 | 待执行 |
| 完整门禁 | 收尾改动引入回归 | Go 专项门禁与 Shell 回归全部通过 | — | 不适用 | 不适用 | 全部测试 | Go 专项门禁、Shell 回归 | 待执行 |

**README：** 不适用（无用户可见行为变化；安全前提写入 `SECURITY.md`，依据设计 §3.3「README 之外的运维与安全文档」）。

## Task 完成记录

- 状态：`待实施 / 实施中 / 待审查 / 已封账`
- 实际范围：
- 规格偏差：`无 / 说明`
- 主提交 SHA：
- 状态封账：`待提交 / 已提交（由 Git 历史和固定提交消息识别）`
- README：`不适用（原因见上）`
- 聚焦测试：
- Task 专项门禁：
- 复用或省略的验证：
- 未解决风险：

---

## 完成检查（每个 Task 交接前）

- 验收矩阵所有适用项已有新鲜命令输出证据，Linux 行为测试记录了 `uname -r`。
- Global Constraints 第 8 条要求的变异证据齐全：补丁、失败测试名与输出、还原后通过输出。
- Go 专项门禁通过，`gofmt -s -l .` 无输出，`GOOS=darwin go build ./...` 通过。
- 涉及 Shell 文件的 Task 已用与 CI 相同的 ShellCheck 版本检查并运行 Shell 回归。
- 既有测试的接线调整已在「规格偏差」逐条列出，断言未删弱。
- 主提交与状态封账提交已按两步封账记录；工作区状态符合根 `AGENTS.md` 的洁净要求。

## 发布后待验证（不属于任何 Task 的完成条件）

- 在 Ubuntu 22.04（5.15）与 24.04（6.8）真机或等价虚拟机上复跑 T4 与 T3a/T3b，记录实际 errno 与权限结果。
- 真机复跑 `backup`、`restore --dry-run`、`restore`、`update`/`rollback`，确认迁移未改变 v0.7 已验证的行为。
