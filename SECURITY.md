# Security Policy

不要在 issue、日志、截图或提交中公开以下内容：节点密码、OBFS 密码、订阅 token、真实服务器 IP、私钥。

发现安全问题时，请通过 GitHub Security Advisory 私下报告，不要创建公开 issue。

## 备份与恢复（`internal/backup`）的安全前提（v0.7.1 起）

`backup`/`restore`/`rollback` 自 v0.7.1 起把文件创建、替换、删除和读取从「按路径解析」改为「相对逐级打开的目录描述符」（`openat` 家族），在每次变更前重新核验祖先链，收紧了 v0.7 遗留的 TOCTOU（校验与写入之间的竞态）窗口。本节写明这套加固实际提供什么、不提供什么——`README.md` 面向使用者，不包含这类运维与安全边界细节，因此单独记在这里。

**无前提成立的结构性保证**（不依赖以下安全前提，测试可证伪）：

1. 可信根（`DestRoot`、`SnapshotRoot`、`BackupDir`、`--out` 的父目录等）之下的任何符号链接——无论在校验前已存在，还是在打开、创建、设权限的任意时点被替换进来——都不会被跟随。
2. 可信根之下的每个名字都相对「从根描述符逐级打开得到的目录描述符」解析，不会经由被替换的路径重新解析。
3. 权限与属主只作用于本工具已打开的文件描述符，绝不按名字作用于可能已被替换的目录项。
4. 失败清理从不递归删除、从不跟随符号链接，因此非空目录不会被失败清理误删。

**需要以下安全前提才成立的保证**（并非无条件）：

> **安全前提**：从可信根到每一个目标文件之间的所有目录，都不得被不可信主体重命名、移动、替换或写入目录项——即这些目录对不可信主体不可写。

这一前提只覆盖可信根**之下**的目录；可信根自身及其祖先目录按设计允许是符号链接、照常被跟随解析，不在本节防御范围内（`--out` 的父目录、`DestRoot`、`SnapshotRoot`、`BackupDir` 等可信根的解析方式本身不变）。

生产部署（以 root 运行，`/etc/singbox-sub-manager`、`/etc/sing-box`、`/etc/caddy`、`/var/lib/singbox-sub-manager` 等目录均为 root 属主、默认权限不允许其他用户写入）满足该前提。**该前提被破坏时**（即某个不可信主体对这条路径上的某个目录拥有写权限），本工具只提供上面第 1–4 条与下述「检测能力」，**不提供**以下三项：

- **A. 写入与删除始终落在可信根之内。** 文件描述符绑定的是 inode 而不是路径；拥有父目录写权限的主体可以把已打开的子目录整体移出可信根，此后相对该描述符的写入仍会成功，并落在根外——没有可移植的操作系统原语能在执行变更的同一时刻确认「该描述符仍在根内」。
- **B. 新建目录的设权限与清理作用于本次创建的那个目录。** 没有可移植原语能原子地「创建目录并返回其描述符」；在这两步之间，攻击者可以把新建目录换成另一个真实目录，本工具无法识别这种替换。
- **C. 失败清理只删除本次操作创建的产物。** 失败清理不执行祖先链复核（复核失败正是它被触发的常见原因），删除前的身份比对与实际删除之间仍可能被替换。

**检测能力（不是保证）**：若某个已打开的目录在「打开之后、变更前的祖先链复核之前」被移出可信根或被替换，且复核执行时该变化仍可观察到，复核会发现并使操作以错误失败，不执行后续的创建、替换、删除或读取。发生在复核完成之后、实际系统调用之前的移动不在检测范围内；复核前被移出又移回的目录也观察不到。这只能收窄攻击窗口，不能消除它，只由上述安全前提覆盖。

依据：`docs/superpowers/specs/2026-09-12-v0.7.1-openat-path-hardening-design.md` 第 3.3 节。
